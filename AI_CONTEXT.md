# GeometricWeather 项目上下文

> 项目记忆与当前状态。编码规范见 [`AGENTS.md`](AGENTS.md)，Claude Code 指令/构建/发布见 [`CLAUDE.md`](CLAUDE.md)。
> 每完成一项任务，在「变更日志」追加一行（中文）。

## 当前目标

- 保持原 UI 风格
- 使用 Kotlin
- 使用 Retrofit
- 使用 MVVM
- compileSdk=35
- minSdk=24（当前实际为 21，仅在关键库强制要求时升级）

## 实现状态

- 当前发布版本：**3.6.1**（versionCode 30601，正式版）。上一发布版 3.6.0（30600，Prerelease）。主分支 `master`（基于 v3.3.6 重建线）。
- 10 个天气源：WEATHERAPI（默认）、OPEN_METEO、METNO（挪威气象局，免 key 全球）、XIAOMI（小米天气，免 key，中国区最全 + 海外走 Accu 后端）、CAIYUN、APIHZ（中国天气网）、CMA（中国气象局）、MF（仅法国）、OWM 可用；**ACCU 的内置 Key 已过期，当前不可用**（见「已知问题」）。加上 COMPOSITE（多源聚合）共 11 项可选。
- 工具链已现代化（见版本矩阵）；RxJava 已全部迁移到 Coroutines；GreenDAO 已迁移到 Room。

### 版本矩阵

| 组件             | 版本   |
| ---------------- | ------ |
| Gradle           | 8.7    |
| AGP              | 8.4.0  |
| Kotlin           | 1.9.24 |
| Compose Compiler | 1.5.14 |
| compileSdk       | 35     |
| targetSdk        | 35     |
| minSdk           | 21     |

## 已完成（里程碑）

- 修复 Gradle 7.3.3 → 7.6.4
- 升级 AGP 7.2.0 → 7.4.2
- 升级 Kotlin 1.5.31 → 1.8.22
- 升级 AndroidX 全套依赖
- 升级 compileSdk/targetSdk 32 → 34
- 移除 CyanogenMod SDK
- 移除 jcenter() 仓库
- 移除 armeabi ABI
- 修复 proguard-rules.pro
- 修复 Widget Intent Filter 拼写
- 替换 BubbleSeekBar → Material Slider
- 配置 GitHub Actions CI/CD
- 发布 v3.102-modernize Release
- 修复 API keys 读取问题 (local.properties 优先)
- 修复坐标系转换 (GCJ-02 → WGS-84)
- 发布 v3.103 Release (3个APK)
- 添加 Open-Meteo 天气提供商 (免费，无需API Key)
- 添加 WeatherAPI 提供商
- 发布 v3.104 Release (含新天气提供商)
- 添加新提供商 API Key 设置界面
- 修复设置界面位置服务下拉菜单 bug
- 内置 API Keys (AccuWeather, OpenWeather, 彩云, WeatherAPI)
- 移除 QWeather 和 Visual Crossing (API不可用)
- 重写"数据提供商"设置页面（分区布局，Section Header）
- 修复 MIUI Activity recreate 崩溃 (ClassCastException BinderProxy)
- 全面 null 安全改造 (CaiYun/AccuWeather/OWM/MF 转换器)
- 修复 MfResultConverter province/country 字段 null 崩溃
- 更换 AccuWeather base URL → dataservice.accuweather.com (api. 域名 key 失效)
- 设置页面顶部栏 MediumTopAppBar → SmallTopAppBar (减少遮挡)
- 修复设置页面 Scaffold innerPadding 未传入 NavHost 导致内容被顶部栏遮挡
- 发布版本切换为 Prerelease
- 优化设置页面滑动性能 (移除不必要的 nestedScroll、Card → Surface、pinnedScrollBehavior)
- 修复彩云天气: 小米市场API失效 → 官方 v2.6 API (api.caiyunapp.com)
- 修复彩云天气: Wind构造器传null导致转换失败 (Hourly/Daily)
- 修复彩云天气: Weather构造器minutely/alert传null导致@NonNull断言失败
- 修复 OpenWeather: One Call API 废弃 → 免费端点 weather/forecast/air_pollution
- 修复 Room 主线程数据库访问崩溃 (20处，10个文件)
  - MainActivityViewModel.init() → AsyncHelper.runOnIO 回调
  - WeatherHelper/LocationHelper 回调中 DB 写入 → IO 线程
  - SearchActivity/WidgetConfigActivity onCreate → 异步加载
  - AllergenActivity Compose → LaunchedEffect + Dispatchers.IO
  - ServiceProviderSettingsScreen → AsyncHelper.runOnIO
  - TileService.refreshTile() → IO 线程
- 工具链升级: Gradle 7.6.4 → 8.7, AGP 7.4.2 → 8.4.0, Kotlin 1.8.22 → 1.9.24
  - Compose Compiler 1.4.8 → 1.5.14
  - compileSdk/targetSdk 34 → 35
  - OkHttp 3.12.12 → 4.12.0
  - Hilt 2.48 → 2.51.1
  - AndroidX 全套依赖升至最新
  - 修复 WorkManager Configuration.Provider API 变更
  - 修复 OkHttp Util.platformTrustManager() 移除
  - 添加 buildConfig true (AGP 8.x 默认关闭)
  - lintOptions → lint
  - 合并 kapt 块，移除废弃的 kotlin-stdlib-jdk7/jdk8 force
- 修复 compileSdk 35 deprecation 警告 (22处，9个文件)
  - DisplayUtils: SYSTEM_UI_FLAG → WindowInsetsControllerCompat
  - PackageManager int-flag → ApplicationInfoFlags/ResolveInfoFlags (API 33+)
  - TimeObserverService: registerReceiver 添加 RECEIVER_NOT_EXPORTED (API 33+)
  - MaterialLiveWallpaperService: getDefaultDisplay().getRefreshRate() → getDisplay().getRefreshRate() (API 30+)
  - MainActivity: getParcelableExtra → 带 Class 参数版本 (API 33+)
  - Location: readSerializable → 带 Class 参数版本 (API 33+)
  - FitSystemBarComposeWrappers: SmallTopAppBar → TopAppBar, ArrowBack → AutoMirrored
- RxJava → Coroutines 完全迁移 (28个文件，-256行代码)
  - AsyncHelper.java → AsyncHelper.kt (Kotlin Coroutines: Job, Dispatchers, delay)
  - 8个 Retrofit API 接口: Observable<T> → Call<T>
  - 6个 Weather Service: 迁移到 AsyncHelper + Call.execute()
    - 简单服务: OpenMeteo, WeatherApi, CaiYun, BaiduIP
    - 复杂并行服务: AccuWeather(6路), OWM(3路), MF(6路) — CountDownLatch + AtomicReference
  - WeatherHelper.requestLocation(): Observable.zip → CountDownLatch
  - 删除: SchedulerTransformer, BaseObserver, ObserverContainer
  - 移除依赖: rxjava, rxandroid, adapter-rxjava2, room-rxjava2
- 修复 AsyncHelper Emitter 回调线程问题
  - Emitter.send() 在非主线程时通过 Handler post 到主线程
  - 匹配原 RxJava observeOn(mainThread()) 行为
  - 修复 "Cannot invoke setValue on a background thread" 崩溃
- GreenDAO → Room 迁移（删除 GreenDAO 依赖、新建 8 个 Room Entity/DAO/Database/TypeConverter、重写 DatabaseHelper）
  - Room 2.6.1，数据库 schema 版本 63
  - 子实体 weatherSource 用 String（写入时 source.getId()）；LocationEntity 用 WeatherSource/TimeZone 强类型（RoomTypeConverters）
  - 本地 Microsoft JDK 17 kapt 的 InvocationTargetException → 加 `kapt.useWorkerApi=false` 解决

## 变更日志（按版本）

### 3.3.6 回退线（分支 rollback/3.3.6，HEAD=v3.3.6）

> 背景：3.3.13 引入了定位"显示默认"等回归，故回退到 v3.3.6 重新开发（3.3.13 的全部改动备份在提交 4054781 与 stash）。在 3.3.6 基础上做有针对性修复，**不带回** 3.3.13 的定位改动（保留 3.3.6 的北京兜底，实测定位更好）。

- 数据提供商中文界面补全：`values-zh-rCN/arrays.xml` 的 `weather_sources`/`weather_source_voices` 补 Open-Meteo、WeatherAPI（4→6）；`strings.xml` 补 3 个分区标题 + 高级页 14 条 API KEY 文案
- 全链路诊断日志：LocHelper（定位/逆地理编码/北京兜底）、WeatherHelper、AccuWeatherService（geoPosition HTTP 码 + 异常）、CaiYunService（HTTP 码 + 解析结果）
- 修复彩云 HTTP 400：移除 `CaiYunSignatureInterceptor`（标准 v2.6 token 接口不需签名，签名头反被拒）；改用 local.properties 的有效 token；删除 CAIYUN_APP_SECRET
- 修复彩云解析崩溃：v2.6 的 `hourly.air_quality`（未用，改 JsonElement 容错）与 `daily.air_quality`（由 List 改为对象 + 同步改 converter）实际是对象不是数组
- 全面空安全（治本）：`Current`/`Daily` 构造器对 @NonNull 但实际可能为 null 的字段（AirQuality/Pollen/Astro sun&moon）强制兜底空对象 → 一次性消除 HeaderViewHolder/MainAdapter/DailyViewHolder/WeatherEntityGenerator/DailyEntityGenerator/Weather.isDaylight 等处的 NPE/越界
- 修复动态壁纸：① onVisibilityChanged 主线程读 Room → AsyncHelper.runOnIO + 空列表兜底 ② getDisplay() 在 WallpaperService(非视觉 Context) 抛异常 → 改用 DisplayManager ③ 绘制 runnable 加 Surface.isValid() + try/finally 防 "Surface has already been released" ④ onDestroy 无条件取消绘制 interval + quitSafely，interval 重建前先 cancel 且仅在线程存活时 post → 消除 12 万条/120s "dead thread" 刷屏
- 实测（小米 HyperOS 真机）：定位解析到天津、彩云 HTTP 200 + 转换 OK、动态壁纸正常渲染、0 崩溃、0 dead-thread 刷屏
- 清理本次调试日志（LocationHelper/WeatherHelper 还原无日志；Accu/CaiYun 去日志保留空安全防护）

### 3.4.x（master）

- **3.4.0**：设为主分支并发布——master 重置到该修复线并 force-push（3.3.7-3.3.13 保留在 tag v3.3.7~v3.3.13 与分支 backup/3.3.13-fixes）；版本 3.3.6 → 3.4.0（versionCode 30400，高于旧 30313）
- **3.4.1**：修复动态壁纸配置页内容被顶栏遮挡（Material3Scaffold innerPadding 未应用到 LazyColumn）
- **3.4.2**：回填 3.3.7–3.3.13 的非定位崩溃修复（不带回定位回归）：MainThemeColorProvider 空安全、MainActivityViewModel 空 validList 越界（首装崩溃）、DefaultResourceProvider 图标缺失 NPE（getDrawable 兜底 SunDrawable）、Baidu/AMap 服务补 foregroundServiceType=location
- **3.4.3**：P3 清理——GeometricWeather.setDayNightMode 的 observeForever 改为稳定 Observer + 先 remove 再加（防累积）；MfResultConverter 的 Europe/Paris 去掉 TODO（MF 仅法国、搜索结果无 tz，真实 tz 随预报返回，故 Paris 正确）
- 新增 Android（16）预测性返回：manifest `<application>` 开 `enableOnBackInvokedCallback="true"`（全 app 生效，绝大多数页面不拦截 back，自动获得预测返回动画）；`SearchActivity`/`AbstractWidgetConfigActivity` 的 legacy `onBackPressed()` 覆写迁移到 `OnBackPressedDispatcher` + `OnBackPressedCallback`（真机验证 "OnBackInvokedCallback is not enabled" 警告消失、0 崩溃）
- 修复搜索位置闪退：`WeatherHelper.requestLocation` 的成功/失败回调在 `Dispatchers.IO` 线程直接触发，上层 `SearchActivityViewModel` 在回调里 `LiveData.setValue()` → 后台线程调用抛 `IllegalStateException` 崩溃（RxJava→Coroutines 迁移时漏掉 `observeOn(mainThread)`，姊妹方法 `requestWeather` 已正确处理）；改为 `AsyncHelper.delayRunOnUI` 回主线程后再回调
- **3.4.4**：修复"只有定位地区能换天气源"——`ServiceProviderSettingsScreen` 切换全局天气源时只重写定位地区（`indexOfFirst { isCurrentPosition }`），手动添加的地区不受影响；改为重设**所有**地区为新源（用旧源删旧缓存、按 formattedId 去重防同城多源塌缩、整表重写后重载自动拉取）；代价：放弃"同城用不同源各存一个"
- **3.4.5**：新增天气源 **中国气象局（CMA, weather.cma.cn）**——无需 Key，基于监测站 ID。流水线 4 层 + 注册：`weather/json/cma/{CmaWeatherResult,CmaLocationResult}`、`apis/CmaApi`（autocomplete/weather.view/逐小时 HTML）、`converters/CmaResultConverter`、`services/CmaWeatherService`；`WeatherSource` 加 CMA、`WeatherServiceSet`/`ApiModule` 注册、`build.gradle` 加 `CMA_BASE_URL`、`values`+`values-zh-rCN` 的 weather_sources/voices/values 加项。要点：① autocomplete 只认拼音/英文 → 中文用 `android.icu.Transliterator`（API24+，<24 透传）转拼音；② `now` 无天气文字 → 取当天 daily 按昼夜推导；③ 逐小时无 JSON → 抓 `web/weather/{id}.html` 的 hour-table 正则解析，图标号经 daily 的 code→text 自洽映射为 WeatherCode；④ 定位地区用 stationid 空（按 IP）兜底；⑤ 风速 m/s→km/h(×3.6)；⑥ 中文文字→WeatherCode 映射。顺带把 `Utils.getName/getVoice` 的 `!!` 改 `?: id` 防御（消除其余 13 个 locale 源名数组缺项的潜在 NPE）
- **3.4.6**：修复 CMA"获取天气数据失败"——① 根因：weather.cma.cn 的 WAF 对默认 `okhttp/4.12.0` UA 返回 **403**（浏览器 UA 返回 200）；给 `CmaApi` 三个接口加 `@Headers("User-Agent: Mozilla/5.0 …Chrome…")`（仅 CMA，不动共享 OkHttp）。② 健壮性：被全局换源切到 CMA 的旧地区 cityId 不是 CMA 站点 ID → `requestWeather` 首次无 daily 时按地名（拼音→autocomplete）/IP 兜底解析站点 ID 重试，逐小时也用实际命中的站点 ID 抓取
- **3.4.7**：修复 CMA 定位错误 + 搜索地点无天气（真机 logcat 定位）——① 定位错：`weather/view?stationid=` 留空走 IP，手机出口 IP 可能跨省（南开→开州）；改为用 GPS 坐标在 `api/map/weather/1`（全国 2439 站点带经纬度，进程内缓存）里找**最近站点**，IP 仅作最后兜底。② 搜索/换源地点无天气：CMA 对非本站 ID 返回 `data:""`（空字符串非对象），Gson 抛异常逃到外层 catch，使之前加的重试从未触发；改用 `tryGetWeather`（吞解析异常→null）+ 坐标/地名/IP 重解析站点重试。autocomplete 仅匹配拼音/英文且模糊，故自动解析一律走坐标最近站点而非取 autocomplete 首条。真机验证：南开 → 最近站 54517 黑牛城（天津），当前/7天/逐小时/0 崩溃
- 维护清理：CMA `resolveStation()` 去重（`requestWeather`/`requestLocation` 共用一套坐标/地名/IP 兜底）；删除无引用的 `LogHelper.java`；移除空的 `common/rxjava/` 目录
- **3.4.9**：中国天气网 APIHZ 接入按城市接口 `tqyb.php`（`sheng`+`place`），补足 `tqybip.php` 仅按 IP 的短板。`ApihzApi` 加 `getWeatherByPlace`（原 IP 法改名 `getWeatherByIp`）；`requestWeather` 改为**按地名取数**：`fetchForLocation` 把地区省/区/市归一化后**依次尝试** 省+地点 → 仅地点 → IP 兜底；`requestLocation(query)` 改为**真实搜索**（`tqyb.php` 仅地点查，命中即按 `sheng/shi/经纬度` 建 `Location`，cityId=省+市）。复用既有 DTO/转换器，无新增源/arrays/枚举。要点（实测接口怪癖）：① `place` 带「区」会 400（`海淀区`✗/`海淀`✓）→ 去尾「区」；② 直辖市 `sheng` 带「市」会 400（`北京市`✗/`北京`✓）→ 去尾「市」（省/自治区保留）；③ 接口**区级覆盖不全**（`海淀`✓ 但 `天河`/`渝中`✗）→ 区不中再退到市（→`广州`/`重庆`），故按 区→市 两级候选；④ 仅地点查最宽松，搜索与兜底都走它；⑤ Retrofit `@Query` 自动 UTF-8 编码中文（Node 实测北京海淀/南京/上海/重庆/广州/深圳均命中，国外退 IP）。已 `assemblePubDebug` 通过
- **3.5.1**：正式发布版（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。汇总本会话：APIHZ(中国天气网)源、定位精确到区县、天气源可用性实测页、WeatherAPI 错地区预警过滤 + 缓存键修复 + 孤儿行清理。真机验证（MI 9，卸 debug 装 release）：启动 0 崩溃、自动定位「南开区」、WeatherAPI 联网拉取并 Gson 解析正常（证明 R8 没误删 DTO——`proguard-rules.pro:20` 的 `weather.json.**` 通配覆盖了新增 `weather.json.apihz`）、当前/体感/AQI/5天预报齐全。
- **3.5.2**：① 默认天气源 ACCU → WEATHERAPI（`SettingsManager.weatherSource` 的 `getString("weather_source", …)` 兜底值）。ACCU 的内置 Key 已过期且暂无新 Key，新装用户首次进 App 会直接落到死源、拉不到天气。全仓库只有 `WeatherSource` 枚举一处 `"accu"` 字面量，无第二处硬编码默认值；`getInstance("weatherapi")` 命中 WEATHERAPI（`contains` 匹配）。仅影响**新装**（老用户 prefs 里已存的源不变）。② `CmaWeatherService.mCalls` 由 `ArrayList` 改 `CopyOnWriteArrayList`：三处 `add()` 都在 `runOnIO` 的 lambda 体内（IO 线程），而 `cancel()` 在调用线程 `for` 迭代同一个裸 list → `ConcurrentModificationException`（刷新中途退出/切换地区时触发）。每次请求只 add 1~3 个，COW 复制开销可忽略。审查 APIHZ 时顺带查出，APIHZ 本身无缺陷（`Base.cityId` 用的 `location.getCityId()`，空值已兜底，回调线程由 `WeatherHelper.java:62` 的 `delayRunOnUI` 统一切回主线程）。`assemblePubDebug` 通过。
- **OpenWeather 三连修 + 空 daily 治本**（真机 MI 9/Android 14 定位）：① **可用性页误判**——`WeatherSourceStatusScreen.kt:58` 的 `CHECK_TIMEOUT_MS` 25s **短于**被测对象自身预算（OkHttp connect/read 30s `TLSCompactHelper:93-94`、各服务 `latch.await(30s)`），国内到 `api.openweathermap.org` 的 **TLS 握手**偶发卡死（栈：`ConscryptEngineSocket.doHandshake` ← `RealConnection.connectTls`，三路并行冷启动各自新建连接，随机某路中招）时 OWM **必然**显示"不可用" → 改 35s。加临时 `Log.e("OWMPROBE")` 探针实测三轮确认：Key 有效（直连三端点均 200）、`convert -> wrapper=ok` 每轮成功，故**取数链路本身没问题**，纯粹是仪器预算比被测对象短（探针已回滚）。② **`data/2.5/find` 类型错配**——返回 `{"list":[...]}` 对象却声明成 `Call<List<OwmLocationResult>>`，Gson 抛 `JsonSyntaxException` 被 `OwmWeatherService:156` 的 catch 吞掉 → OWM 源下**搜索城市永远空列表**。改用 `geo/1.0/direct`|`reverse`（返回扁平数组，字段 `{name,lat,lon,country}` 与现有 DTO **完全一致**，故只改 URL + 加 `limit` 参数，**无需包装类**，比加 `OwmFindResult` 更省）；顺带删无调用者的 `getWeatherLocation`。③ **空 daily 列表越界（治本）**——切到 OWM 后主界面与城市管理页**必崩**（`DetailsAdapter:103`、`LocationModel:45` 的 `IndexOutOfBoundsException`）。全仓库扫出 **76 处** `getDailyForecast()/getHourlyForecast().get(0)` 无保护，**不逐处打补丁**，改在两个唯一汇聚点拦：`WeatherHelper.requestWeatherSuccess`（空 daily 视为失败 → 保留旧缓存，而非展示空列表）+ `DatabaseHelper.readWeather`（空 daily 行返回 null，挡住守卫上线前已落库的脏数据）。④ 顺带修 `SelectProviderActivity` 缺注册 `WeatherSourceStatus` 路由——`ServiceProviderSettingsScreen` 被 `SettingsActivity`/`SelectProviderActivity` **两个** Activity 复用，3.4.11 加页面时只注册了前者，从主界面滑动进的那条路径点「天气源可用性」必崩（`IllegalArgumentException: destination ...source_status cannot be found`，崩溃日志里 `startDestination=...providers` 即指认了是哪张图）。真机验证：OWM 实测 **6天/40点 可用**、切 OWM 后主界面+城市管理 0 崩溃、搜索 Shanghai 返回 5 条带正确 `CN`/`IN` 国家码结果。**注**：TLS 握手超时本身是国内网络环境问题、非代码缺陷，改超时只是让仪器别误报。
- 测试：新增 `app/src/test/java/weather/converters/ApihzResultConverterTest.java`（Robolectric，4 个用例）+ 2 份固件 `src/test/resources/apihz/{tqyb_full,tqyb_sparse}.json`。锁住转换器两类历史缺陷：① `Base.cityId` 必须来自 `location.getCityId()` 而非接口地名（3.4.13/3.4.14 的缓存键漂移）；② 稀疏但合法的 200 响应不得触发 `@NonNull` 断言（外层 catch 会把崩溃悄悄变成"无数据"，正是这类回归长期隐身的原因）。另覆盖满响应 7 天 / 12 逐时 / 风速 m/s→km/h(×3.6) / 高温橙色预警 1 条 / 非 200 码与 null 入参返回 null。踩到两个坑：① 真实模型签名是 `Temperature.getTemperature()` 返回原始 `int`（不可 `.intValue()`）、`Daily.sun()`（非 `getSun()`）、`Current.getRelativeHumidity()` 返回 `Float`；② Robolectric 4.12.2 不带 SDK 35 沙箱，targetSdk 35 直接抛 `DefaultSdkPicker` IllegalArgumentException → 类上加 `@Config(sdk = 34)`。`:app:testPubDebugUnitTest` 4/4 通过，`ApihzResultConverter` 本体未改动。（Windows 下用 python 跑 gradle 须 `decode('utf-8')` + `sys.stdout.reconfigure(encoding='utf-8')`，否则中文 javac 报错撞 GBK 解码异常看不到真实错误行）
- **Météo France 整源修复（v1 DTO → GeoJSON v2）**：MF 早已把 `webservice.meteofrance.com` 迁到 GeoJSON（字段全挪到 `properties` 下并改名），仓库里的 DTO 还是 v1 扁平结构 → Gson 解出**全 null 对象** → 转换器踩 `@NonNull` 抛异常被外层 catch 吞成"无数据"，**MF 整个源不可用**（法国用户全灭）。① DTO：删 `MfForecastResult`/`MfHistoryResult`（v1 遗物），`MfCurrentResult` 改 `properties.gridded`、`MfRainResult` 改 `properties.forecast`（`rain_intensity`：1=无雨），统一走已有的 `MfForecastV2Result`。② `MfWeatherApi`：清掉 5 个指向同一 `/v2/forecast` 的死方法（`getForecastV2`/`getForecastInstants`/`getForecastInseepp`/`getWeatherLocation`）；地名搜索改 `places`（**不在 `/v2` 下**，`/v2/forecast?q=` 返的是单个 forecast feature 不是列表）；预警 `/v3/warnings` → **`/v3/warning/full`**（前者实测 404 `"You haven't access to this url"`）。③ `ApiModule`：MF 专用 Gson——MF 一律 UTC ISO-8601（`2026-08-10T09:00:00.000Z`），共享 Gson 的 `yyyy-MM-dd'T'HH:mm:ss` 会**静默丢掉 `.000Z` 并按设备本地时区读**，所有时间整体偏移设备 UTC offset（`'X'` 需 API24，minSdk 21 用不了，故手写两 pattern + `setTimeZone(UTC)`）。④ `MfWeatherService`：`latch` 6→5，预警**串在 forecast 之后**——`domain` 要的是部门号（`75`），只有 forecast 响应带 `french_department`，原来传 `location.getProvince()`（省名/大区名）→ 预警必然拿不到；AtmoAura 那串 13 个 `equals` 收进 `isAtmoAuraDepartment()`，department 兜底仅在 province 形如 `\d{2,3}[AB]?` 时采用。⑤ 转换器：空 daily 直接返回 `wrapper(null)`（配合全局"空 daily 视为失败"约定）、跳过 MF 尾部无温度/无时间的填充条目、`wind_direction=-1` 记为 variable、风速 m/s→km/h(×3.6)。⑥ `WeatherService.WeatherResultWrapper` 加 `getResult()`（原来只有包私有字段，测试读不到）。**实测**（curl 直连 7 个端点）：`v2/forecast`（巴黎 15 天/95 逐时/64 概率）、`v2/observation`、`v3/nowcast/rain`、`v2/ephemeris`、`v3/warning/full`、`places` 全 200，`v3/warnings` 404 确认。**遗留未改**：`daily_forecast[].time` 是 **UTC 午夜**（就是个日期标签），昼夜切分按 `dayStart+6h..+18h` 算即 UTC 06–18，对巴黎夏令时实际是本地 08–20；与 v1 行为完全一致，属上游语义，本次不动。**真机验证**（MI 9/Android 14，设置→数据提供商→天气源可用性→实测刷新，MF 用巴黎坐标走 App 自己的 `WeatherServiceSet`）：Météo France 由「整源不可用」变为 **可用 15 天 / 76 逐时 / UV ✓ / 日出 ✓ / 预警 1 条**、0 崩溃。两处数字正好对上线上载荷，说明改动按预期生效：① 76 = 线上 95 条逐时里剔掉 19 条无 `T` 的填充条目；② 预警 1 条 = `domain=75` 的黄色 Canicule（`color_id=2>1`），旧代码传 `location.getProvince()`（"Île-de-France"）这一列必然是 0。体感/气压/湿度/降水 显示 ✗ 属实——`v2/observation` 只返 T/风/天气文字（实测响应仅 336 字节），逐时里的 `relative_humidity`/`P_sea` 目前没回填进 `Current`（转换器里原有 TODO，本次未做）。
- 测试：新增 `app/src/test/java/weather/converters/MfResultConverterTest.java`（Robolectric，6 个用例）+ 5 份**真实抓取**固件 `src/test/resources/mf/{forecast,observation,rain,ephemeris,warning}.json`（巴黎，仅截短长度）。锁住这次的根因与两处易回归点：v2 载荷必须完整转换（4 天/26 逐时/当前 29℃/UV 6/日照 14~15h）、时间戳必须按 UTC 解析（回退到共享 Gson 立刻挂）、`rain_intensity` 分钟级步长 5 分钟、`phenomenon_max_color_id>1` 才成预警（绿色丢弃）、可选调用（observation/rain/warnings）全 null 仍出 Weather、空 daily 必须判失败。踩到的坑：`Base` 的第 3/4 参是 **publish**（provider 的 `update_time`）、第 5/6 参才是 **update**（设备刷新时刻，`new Date()`），断言 provider 时间要查 `getPublishDate()`——错查 `getUpdateDate()` 会拿到当前时间（与 Accu 等其余转换器同一约定）。`:app:testPubDebugUnitTest` MF 6/6 + APIHZ 4/4 通过，主源码零改动地跑通编译。**注**：全量 `./gradlew test` 另有 4 个**既存**失败（`basic.option._utils.UtilsTest`/`UnitUtilsTest`/`CardDisplayTest`/`DailyTrendDisplayTest`）——PowerMock 在 JDK 17 下 `InaccessibleObjectException: module java.base does not "opens java.lang"`，与本次改动无关（**已在后续「干掉 PowerMock」一条里修掉，现全绿**）。
- **MF 当前天气补全（湿度/气压/体感/降水）**：`v2/observation` 只返 T/风/天气文字（实测响应仅 336 字节），所以可用性页里 MF 的 体感/气压/湿度/降水 四列全是 ✗。这些字段其实都在 `v2/forecast` 的逐时里（`relative_humidity`/`P_sea`/`T_windchill`/`rain_1h`+`snow_1h`），改法：`MfResultConverter` 加 `getNearestHour()`，按**观测时刻**（`gridded.time`，缺则 now）找最近一条逐时，回填 `Current` 的 `relativeHumidity`/`pressure`/`realFeelTemperature`+`windChill`/`precipitation`（复用已有的 `getHourlyPrecipitation`）/`cloudCover`；温度与天气文字仍以观测优先，**观测缺失时退到最近逐时**（原来会显示 0°）。顺带把逐时的 `T_windchill` 同时写进 `realFeelTemperature`——`HourlyWeatherDialog:72` 只读 realFeel，否则「当前」有体感而「逐时详情」没有，同一个源字段两处不一致。落实了转换器里原有的 `TODO: Fill in with observation data instead`。测试加 2 例（共 8 例）：① 固件观测在 11:55Z → 最近步是 12:00Z（`T_windchill` 32.2→32、湿度 35、气压 1017.9），能区分出"取 hour[0]"（09:00Z→28）和"取 11:00Z"（→30）两种错法；② 无观测时温度不得为 0、文字不得为空。真机验证（MI 9，天气源可用性→实测刷新）：**Météo France 可用 15 天/76 逐时，体感✓ UV✓ 气压✓ 湿度✓ 降水✓ 日出✓ 预警 1**，四个 ✗ 全部转 ✓、0 崩溃。AQI 仍 ✗ 属实（只有 Auvergne-Rhône-Alpes 的 AtmoAura 有值，巴黎不在范围内）。同轮 OpenWeather 显示不可用，是已知的国内 TLS 握手偶发超时（上一轮同一台机子是可用），与本次改动无关。
- **3.5.3**：发布版（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。汇总本次三件事:① **Météo France 整源修复** —— MF 早已迁到 GeoJSON v2 而 DTO 还是 v1 扁平结构，Gson 解出全 null → 转换器抛异常被外层 catch 吞成「无数据」，法国用户整源不可用;修 DTO/接口路径(`places`、`/v3/warning/full`)、MF 专用 UTC Gson、预警改用 forecast 带回的 `french_department` 当 domain。② **MF 当前天气补全** —— 按观测时刻取最近逐时回填 湿度/气压/体感/降水/云量，观测缺失时温度退到最近逐时(原来显示 0°)。③ **测试基础设施** —— 删掉 PowerMock(JDK 17 下必挂)，四个用例迁到 Robolectric + Mockito，`./gradlew test` 六变体全绿、29 例 0 失败。真机验证见各自条目;发版前另做 release 包冒烟(见下)。**release 包冒烟（MI 9/Android 14，卸 debug 装签名 release）**：冷启动 0 崩溃、`logcat -b crash` 空、定位到**舒城县**（区县级）、联网拉取正常——先后经 WEATHERAPI（英文 "Light rain shower"）与 APIHZ（中文「中雨」+ 舒城县本地预警）两个源各拉一次，证明 R8 没误删 Gson DTO；再进「天气源可用性」实测，**Météo France 在 R8 下同样是 可用 15 天/76 逐时 体感✓ UV✓ 气压✓**，与 debug 一致（`proguard-rules.pro:20` 的 `weather.json.**` 通配覆盖了改动后的 `weather.json.mf`）。同轮 OWM 不可用（已知 TLS 抖动）、ACCU 不可用（Key 过期）。**注**：本机重装后天气源自动回到「中国天气网」而非 3.5.2 的新装默认 WEATHERAPI，是 MIUI 对同包名 `allowBackup` 数据的自动恢复，不是默认值失效。
- **测试基础设施：干掉 PowerMock,`./gradlew test` 恢复全绿**。`basic.option._utils.UtilsTest`/`basic.option.unit.UnitUtilsTest`/`basic.option.appearance.CardDisplayTest`/`DailyTrendDisplayTest` 四个用例长期挂在 `InaccessibleObjectException: Unable to make protected native java.lang.Object.clone() accessible: module java.base does not "opens java.lang"` —— PowerMock 2.0.9 靠 javassist 改写 + 自定义 ClassLoader,撞 JDK 17 的模块封装,不是我们的代码问题。**没走 `--add-opens` 那条路**(能糊住一个 open 还会冒下一个,而且把一个已停更的依赖继续养着,JDK 再升一次又炸),改用仓库里**已经装着**的 Robolectric + Mockito,然后**删掉 4 个 PowerMock 依赖**(`powermock-api-mockito2`/`module-junit4`/`module-junit4-rule`/`classloading-xstream`)。逐个改法:① `CardDisplayTest`/`DailyTrendDisplayTest` 原来 `mockStatic(TextUtils.class)` 把 `isEmpty` 恒定打桩成 false,换 `@RunWith(RobolectricTestRunner.class) @Config(sdk = 34)` 后 **TextUtils 是真的**,静态打桩整段不需要了,`Context` 用 `Mockito.mock` 即可(`CardDisplay.getName` 只调 `context.getString(nameId)`);顺带各加一例空串→空列表,把原先被打桩抹掉的 `TextUtils.isEmpty` 分支真正测上。② `UtilsTest` 只 mock `android.content.res.Resources`(普通类,非 static/final),`@RunWith(MockitoJUnitRunner.class)` 就够,不碰 Android 运行时;加一例"值不在数组里→返回 null"(调用方靠它退回原始 id)。③ `UnitUtilsTest` 更离谱:它 `@RunWith(PowerMockRunner.class)` 但**一个 mock 都没用**,而且断言的是**它自己文件里定义的** `formatFloat`/`formatInt` 局部副本 —— 等于在测 JDK 的 `String.format`,对 App 零覆盖。改成指向真的 `Utils.INSTANCE.formatFloat/formatInt`,并补一例整数值不带小数(`14f`→`"14"`)——那正是真实现有、局部副本没有的分支。结果:`./gradlew test` 六个变体(pub/gplay/fdroid × debug/release)全部 BUILD SUCCESSFUL,`testPubDebugUnitTest` **29 例 0 失败**(原 25 例 6 失败),用例数 25→29 且全部真在测 App 代码。
- **3.4.15**：一次性清理 3.4.14 之前残留的孤儿 weather 行（按接口地名 Tianjin/Wangdingdi/Shuchenghsien 或旧源 cityId 存的、不属于任何已存地区的行）。`WeatherDatabaseDao` 加 `@Query("DELETE FROM weather WHERE cityId NOT IN (SELECT cityId FROM location)") deleteOrphanWeather()`、`DatabaseHelper.cleanupOrphanWeather()`、`GeometricWeather.onCreate` 主进程内 `cleanupOrphanWeatherOnce()`（`app_init` 偏好 `orphan_weather_cleaned` 一次性守卫 + `AsyncHelper.runOnIO`）。只清 weather 表（daily/hourly/alert/history 本就按 `location.getCityId()` 存，无孤儿）。真机验证：weather 行 36(6 个 cityId)→1(仅 54517_tj)、0 崩溃。注意：验证时 DELETE 落在 `-wal` 里，必须连 `-wal`/`-shm` 一起 pull 才能看到合并后的真实状态（只 cat 主 db 文件会看到旧数据，是假象）。
- **3.4.14**：修复 3.4.13 遗留的 WeatherAPI 缓存键错配。`WeatherApiResultConverter.convert` 里 `Base.cityId` 改用 `location.getCityId()`（原来用 `result.location.name`=Tianjin/Wangdingdi… 随接口地名漂移）。根因：`WeatherEntityGenerator` 按 `weather.getBase().getCityId()` 存 weather 行，而 `readWeather`/`deleteWeather` 按 `location.getCityId()` 读删 → 键对不上：① WeatherAPI 天气**永远读不到缓存**（冷启动每次重新拉）；② weather 表按接口地名无界累积。改后与其余所有转换器一致（CMA/APIHZ 本就用 `location.getCityId()`）。真机验证：刷新后 weather 行落到 `54517_tj`，连刷两次稳定 1 行（不再累积）、预警仍 0、0 崩溃。旧的 Tianjin/Wangdingdi 孤儿行是修复前残留，不再增长、不被读取（未做迁移清理）。
- **3.4.13**：修复「定位南开区但预警显示延庆区」。双根因：① **WeatherAPI 接口数据张冠李戴**——对天津坐标返回了北京延庆区预警（`areas=北京市`、`headline=延庆区气象台…`，接口自己 `location` 都识别成 Tianjin 却附了北京预警）。在 `WeatherApiResultConverter.convertAlertList` 加按行政区过滤：`areas` 非空、且 `areas+headline` 既不含本地 province 也不含 city 词根（去掉 省/市/自治区/特别行政区/地区/盟 后缀）时丢弃该预警（只在 isChina 且有地名可比时才判，避免误删）。② **`DatabaseHelper.deleteWeather` 漏删 alert 表**（只删了 weather/daily/hourly/minutely/history）→ 每次刷新预警累积（实测同一条延庆预警攒了 26 份）。补 `selectAlertListByCityIdAndSource`+`deleteAlertList`，对所有源生效。真机验证（MI 9，weatherapi 源）：南开区刷新后 `(54517_tj,weatherapi)` 预警 26→0、天气页无预警横幅、头部显示「南开区」27°、0 崩溃。**遗留 BUG（未改）**：`WeatherApiResultConverter` 把 `Base.cityId` 设成 `result.location.name`（Tianjin/Wangdingdi…随接口地名变）而非 `location.getCityId()`，致 weather 表按接口地名累积、与 `deleteWeather` 按 cityId 删除不匹配（weather 行无界增长）；alert 走 `location.getCityId()` 不受影响，故本次预警修复完整。
- **3.4.11 / 3.4.12**：设置里新增「天气源可用性」页（`settings/compose/WeatherSourceStatusScreen.kt`），横向滚动表格。**3.4.12 改为实测版**：页面带「实测刷新」按钮，点了才**复用 App 的 `WeatherServiceSet` 真实联网**——每个源走 `requestLocation→requestWeather`（与 App 同一流程；ACCU 用 `location.getCityId()` 当 Key 必须先 resolve，坐标源 requestLocation 直接 echo），从返回的 `Weather` 模型抽取 天/时/体感(realFeel)/UV(isValidIndex)/气压/湿度/降水/AQI(isValid)/预警条数/日出，逐源带 25s 超时 + generation 守卫防重入。`WeatherServiceSet` 经 Hilt `@EntryPoint`(`EntryPointAccessors.fromApplication`) 取得（无 hilt-navigation-compose 依赖）。测试点：北京坐标，MF 用巴黎。入口 设置→数据提供商→天气源可用性（`ServiceProviderSettingsScreen` 加 `clickablePreferenceItem`；`SettingsScreenRouter`+`SettingsActivity` NavHost 注册；strings 加 `settings_title/summary_weather_source_status`）。注意：每次刷新真实消耗各源配额；预警列是「当前生效条数」（多数地区 0 条不代表不支持）。真机实测全部联网成功（UV/AQI/日出放宽为 current/daily/hourly 任一）、CMA 抓到逐小时 56 点、国际源偶因 25s 超时显示不可用。`assemblePubDebug` 通过、真机 0 崩溃
- **3.4.10**：定位精确到区县。定位 SDK（百度/高德，`setIsNeedAddress`/`setNeedAddress` 本就开着）回传的 省/市/区 之前被 `LocationService.Result` 丢弃（只留经纬度）→ 当前定位只到市级（「天津市」）。修复：`Result` 加 `country/province/city/district`（`@JvmOverloads constructor` 兼容其余服务的旧 2 参构造）；`BaiduLocationService`/`AMapLocationService` 回填这四项；`LocationHelper` 把 `Location.copy(lat,lon,tz)` 换成实例 `location.copy(null,lat,lon,tz,country,province,city,district)`（copy 对 null 入参保留原值，所以只覆盖非空地址）。`getCityName` 本就优先显示 district → 头部显示「南开区」。真机验证（MI 9，百度定位）：CURRENT_POSITION province/city=天津市、**district=南开区**、0 崩溃。注意：① 原生定位 `AndroidLocationService` 的回调在 `Looper.getMainLooper()` 主线程，不能阻塞调 `Geocoder`，故原生只给坐标→市级，区县级需用 百度/高德定位；② 坐标型源(WeatherAPI/Open-Meteo/OWM)天气本就按 GPS 精确，名称受定位服务影响；③ APIHZ/CMA 无区县级天气数据（apihz `place=南开`→400，回退到市/最近站），区只影响显示名
- **仓库瘦身：清掉冗余文件（无功能改动）**。① **旧构建产物出库 68.5 MB** —— `release/3.102/{pub,gplay,fdroid}/release/*.apk` 与 `app/{pub,gplay,fdroid}/release/*.apk` md5 完全相同（同一批上游 3.102 的包存了两份）、外加 `app/release/app.aab` + `output.json`/`output-metadata.json`；`.gitignore` 里本就有 `*.apk` 但对**已跟踪**文件无效，故一直躺在库里。已 `git rm` 并把 `/release/`、`app/*/release/`、`*.aab`、`output*.json` 补进 `.gitignore`。**注**：历史里的 blob 仍占着 `.git`（161 MB），除非重写历史（不做）。② **死代码 5 个文件** —— `common/di/RxModule.java`（RxJava 移除后剩的空 Hilt module，零 `@Provides`）、`common/retrofit/interceptors/CaiYunSignatureInterceptor.java`（从未实例化；`RetrofitModule.java:26` 注释已写明 v2.6 接口带签名头会 400）、`common/utils/ObjectUtils.java`（`deepCopy` 无调用方）、`weather/json/owm/{OwmOneCallResult,OwmOneCallHistoryResult}.java`（24 KB DTO，`OwmApi` 只用 current/forecast/airPollution/location 四个端点）。③ **未引用布局 4 个** —— `activity_allergen.xml`（`AllergenActivity.kt` 已 Compose 化）、`container_alert_display_view.xml`、`spinner_text.xml`、`menu/activity_settings.xml`（设置页已 Compose 化）；判据是按 ViewBinding 生成类名 + `R.layout`/`R.menu` 双向核对，非"文本零命中"。④ 开发残留 `error_log.txt`（UTF-16 乱码的旧编译日志，且已在 `.gitignore` 却仍被跟踪）、`opencode.json`（指向 `127.0.0.1:4000` 的第三方 CLI 个人配置）。⑤ 未跟踪垃圾就地删：13 个 `hs_err_pid*.log`(2.9 MB)、`GeometricWeather-v3.2.1_pub.apk`(16 MB)、空 `logs/`、`app/build/`(2.1 GB)；两份接口 PDF（中国气象局 / apihz）**未删**，移到 `D:\Documents\geoweather-refs\`（未跟踪、删了不可恢复的参考资料）。**刻意没动的**：`res/drawable` 440 个 + `animator` 36 个静态零引用**是假阳性** —— `ResourceHelper.java:137` 拼 `notif_temp_*`、`DefaultResourceProvider`/`IconPackResourcesProvider` 走 `getResId(ctx, name, "drawable"/"animator")` 按名字动态解析，删了编译照过、运行时才崩；`RetrofitModule`/`UtilsModule` 同样"无文本引用"但是注解处理器消费的活模块；`app/libs/BaiduLBS_Android.jar` + 8 个 `.so`（`LocationHelper.java:51` 真在 new `BaiduLocationService`）；`work/`（249 MB 上游 PSD 设计源，全部被跟踪，是库里最大一块可移出内容，但删它等于丢上游原稿，留待决定）；`fastlane/metadata/**/changelogs/` 虽只到 `30013`（当前 30503）但 F-Droid 需要该目录结构。顺带修 `CLAUDE.md` 两处陈旧描述（`common/rxjava/` 空包和根目录 `index.html` 都已不存在）。**验证**：`./gradlew test` 六变体全绿、**174 例 0 失败**；`assemblePubDebug` BUILD SUCCESSFUL 并产出 `GeometricWeather-v3.5.3_pub.apk`。

- **补齐 4 个转换器测试，顺带查出并修掉 8 个真实缺陷**。给 OPEN_METEO / WEATHERAPI / OWM / CAIYUN 各写一套 Robolectric 测试（24 例）+ 真实抓取固件 `src/test/resources/{openmeteo,weatherapi,owm,caiyun}/`（北京/天津南开点，按各服务真实请求的参数抓，仅截短长度）。写测试过程中查出的缺陷（**都已修，测试断言改成修后的正确行为**）：① **WeatherAPI 日出日落落在 1970-01-01** —— astro 只给 `"05:21 AM"` 裸时钟、日期在上一层的 `forecastday.date`，原来只解析时钟 → `Weather.isDaylight()` 拿 now 跟 1970 的日落比，**恒返回 false**；而它是 3.5.2 起的默认源，主界面 `MainThemeColorProvider`、全部 widget、通知、动态壁纸都读这个值 → 全天候夜间配色。改为把时钟锚到当天日期（按 location 的时区），月出月落同理。② **CaiYun 同一类问题** —— `"05:20"` 裸时钟 → 1970，改为锚到 astro 自带的 `date` + payload 的 timezone。③ **CaiYun 湿度/气压单位** —— 接口给的是 0..1 的湿度分数和**帕斯卡**气压（0.76 / 100430.02），模型要百分比和 hPa → 显示成「湿度 0.76%、气压 100430」，改为 ×100 / ÷100。④ **OWM 风速** —— metric 单位下接口返 m/s，转换器当 km/h 直传 → 风速少报 3.6 倍（连带风力等级偏低），current/daily/hourly 三处都补 ×3.6。⑤ **OWM 能见度** —— 接口返米（10000），模型按公里 → 显示「10000 km」，改 ÷1000。⑥ **OWM 降水概率** —— `pop` 是 0..1，模型存百分比 → 100% 的雨显示成 1%，daily/hourly 都 ×100。⑦ **OWM `rain.3h` 恒 0** —— JSON 键是 `"3h"`（Java 标识符写不出），DTO 只声明了 `double _3h` **没加 `@SerializedName("3h")`** → Gson 从来没填过，逐时降水量永远 0 mm。⑧ **OWM 逐时昼夜恒为 true** —— `new Hourly(..., true, ...)` 写死，凌晨 3 点也画太阳图标；改用接口自带的 `sys.pod`（"d"/"n"），缺失时退到 6..18 本地小时（与 daily 分桶同一规则），DTO 补 `SysBean`。另外两处「抓了不用」也一并接上：**OWM 的 `air_pollution` 结果**（服务并行拉了第三路，转换器收下参数却传 `null` 给 `Current` 的 airQuality）现在按 WeatherAPI 的口径映射成 `AirQuality`（index 用 `main.aqi`，pm2.5/pm10/so2/no2/o3/co 照填）；**Open-Meteo 的 `daily.sunrise/sunset`**（service 请求了、DTO 有字段、转换器传 null）现在填进 `Daily` 的 Astro。测试覆盖各源的：cityId 必须来自 `location.getCityId()`（3.4.14 那类缓存键漂移）、稀疏但合法的 200 响应不得触发 `@NonNull` 断言、单位换算、天气码映射、日出日落是真实 instant；WeatherAPI 另锁 3.4.13 的**跨行政区预警过滤**（固件正好抓到线上现象：给天津坐标返回了河北**霸州市**暴雨红警 → 必须丢弃；把 `areas` 改成天津则必须保留，防止「过滤」被写成「全删」）；OWM 另锁 3 小时步长**按本地日分桶**（固件 10 条正好是 2 个完整的 Asia/Shanghai 日：2 条尾日 + 8 条整日）和空 forecast → 空 daily（配合 `WeatherHelper`/`DatabaseHelper` 两处全局守卫）；CaiYun 另锁 v2.6 的 `daily.air_quality` 对象形状、minutely/alert 必须是空列表而非 null。`./gradlew test` 六变体全绿、`testPubDebugUnitTest` **53 例 0 失败**（原 29 例），`assemblePubDebug` BUILD SUCCESSFUL。**未做**：CMA 与 ACCU 的测试 —— 前者 `weather.cma.cn` 的 WAF 在本机直接掐 TLS 握手（curl/python 均 `UNEXPECTED_EOF`，手机上正常），后者 Key 已过期，都抓不到真实固件，按「不写靠猜的固件」跳过。**已知未修**：CaiYun 的 `?alert=true` 是白请求 —— `CaiYunWeatherResult` 里**根本没有 `alert` 字段**、转换器 alertList 恒 `new ArrayList<>()`；本次固件没抓到预警，形状只能靠猜，故不动。

- **AQI 量纲修正：WeatherAPI/OWM 由「档位号」改为真实中国 AQI**。`AirQuality.aqiIndex` 在全 app 都按 **0–500 的中国 AQI** 消费：`getAqiColor` 阈值 50/100/150/200/300、主界面 AQI 环 `container_main_aqi.xml` 是 `max=400` 且**直接把该数字显示出来**、日详情 `AirQualityHolder` 显示 `aqi + " / " + aqiText`；标准是**中国**而非美国 —— 彩云转换器取的是 `aqi.chn` 不是 `usa`（同一固件 `chn=25 优`/`usa=66 良`，差一档），且 `getPm25Color` 的 35/75/115/150/250 是 GB 3095-2012 限值（EPA 为 12/35.4/55.4）。彩云（`aqi.chn`）与 Accu（`aqiResult.Index`）本就合规，**只有 WeatherAPI 塞 `us-epa-index`(1–6)、OWM 塞 `main.aqi`(1–5) 这两个档位号** → 六档全部 ≤50 落进第一档，**空气再脏也恒为绿色**（实测枚举 1~6 全部 `colorLevel_1 #72d572`），且日详情显示成无意义的 "2 / 2"。修法：① `CommonConverter` 加 `getAqiIndexFromConcentration(pm25, pm10)`，按 HJ 633-2012 分段线性插值、取各污染物子指数最大值，分段表与 `getPm25Color`/`getPm10Color` **同源**（否则同一张卡上「数字」和「颜色」互相打架）；② 两个源改用浓度算 index、`aqiText` 走既有的 `CommonConverter.getAqiQuality()`（与彩云/Accu 一致，显示「41 / Fresh air」而非「2 / 2」）；③ `us-epa-index` 仅在**无浓度**时兜底，映射到档位**中值**（取上界会让「良」顶到 100，显得比实际严重）—— OWM 不需要同类兜底，它的 `components` 一旦非空浓度就是基本类型必有值，那条路走不到，故**没写**（避免死代码）。④ 顺带修 `AirQualityHolder.java:33` 的**拆箱 NPE**：`int aqi = airQuality.getAqiIndex()` 而 `getAqiIndex()` 标了 `@Nullable`，入口条件 `DailyWeatherAdapter:100` 的 `isValid()` 只要**任一**字段非空就通过 → 「有 pm2.5 但没有 index」的响应必崩。这是**既存**隐患（非本次改出），但本次会让 index 为 null 更常见，故一并修；同时 text 为空时不再渲染成 "0 / null"。**取舍（已写进代码注释）**：index 只用 PM2.5/PM10 算 —— 中国 AQI 这两项是 **24h 均值**，气体项另有 1h/24h 两套限值，而接口给的是**瞬时浓度**，严格说对不上任何一套；国内首要污染物绝大多数时候也是这两项。其余气体照填进 `AirQuality` 供分项颜色显示但不参与 index。**故结果是指示性的，不等同官方发布值**。**实测对照**（真实固件，天津南开点）：WeatherAPI `us-epa-index=2`/`pm2.5=29.0`/`pm10=32.6` → 旧显示 "2" 🟩，新 **41 / Fresh air** 🟩（该点位空气本就干净，**颜色是巧合正确的**，错的是数字；注意按 EPA 算同一浓度是 87 会显示黄色，但本 app 用中国标准，41 属「优」，绿色才对）；OWM `main.aqi=2`/`pm2.5=11.12` → 旧 "2"，新 **20**。测试：新增 `CommonConverterTest`（7 例，锁分段边界/段内插值/取最大子指数/超上限封顶/缺值退 null/档位中值/越界档位），并把 OWM 与 WeatherAPI 两处原本断言 "2" 的用例改成修后的正确值。`./gradlew test` 六变体全绿、**60 例 0 失败**（原 53），`assemblePubDebug` BUILD SUCCESSFUL。**真机验证**（MI 9/Android 14，debug 包，舒城县）：WeatherAPI 刷新后 `pm2.5=16.3`/`pm10=16.3` → 主界面 AQI 环显示 **23 / 优**（=max(IAQI_pm2.5 23, IAQI_pm10 16)，旧代码是档位号 "2"），环 `max=400` 下弧长与配色正常；头部由档位号变为「空气质量 - 优」。`AirQualityHolder` 改走**彩云**验证 —— WeatherAPI 的 `forecastday[].day` **根本没有 `air_quality` 键**（只有 `current` 有，固件已确认），故其日详情本就不渲染 AQI 卡，与本次改动无关；切彩云后日详情显示 **21 / 优**（`aqi.chn`）正常。全程 `logcat -b crash` 空、0 崩溃，验完已切回 WEATHERAPI。
- **3.5.4**：发布版（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。汇总自 3.5.3 以来的三件事：① **仓库瘦身** —— 出库 68.5 MB 旧构建产物、5 个死代码文件、4 个未引用布局，无功能改动。② **4 个转换器补测 + 修掉 8 个真实缺陷**（OPEN_METEO/WEATHERAPI/OWM/CAIYUN），其中最要紧的是 **WeatherAPI 日出日落落在 1970-01-01** —— astro 只给裸时钟、日期在上一层，原来没锚日期 → `Weather.isDaylight()` **恒 false**，而它是 3.5.2 起的默认源，主界面配色/全部 widget/通知/动态壁纸**全天候走夜间**；其余为彩云湿度气压单位、OWM 风速×3.6/能见度÷1000/降水概率×100/`rain.3h` 漏 `@SerializedName` /逐时昼夜写死 true。③ **AQI 量纲修正** —— WeatherAPI 的 us-epa-index(1–6) 与 OWM 的 main.aqi(1–5) 是档位号，被当 0–500 的中国 AQI 用 → 六档全落第一档、空气再脏也绿；改为按 HJ 633-2012 用浓度换算。**release 包冒烟（MI 9/Android 14，卸 debug 装签名 release，全新数据）**：冷启动 0 崩溃、`logcat -b crash` 空；新装默认源确为 WEATHERAPI 并联网拉取成功（证明 R8 没误删 Gson DTO）；**AQI 环 28 / 优**（`pm2.5=19.8`→IAQI 28、`pm10=19.8`→IAQI 20，取大者 28），量纲修正在 R8 下生效；**日出 05:36 / 日落 18:57**（月出 05:34 / 月落 19:14）为真实时刻而非 1970，astro 修复在 R8 下生效。**一处易误判**：全新安装时「位置服务」默认是**原生接口**，它只回坐标不回地址（回调在主线程不能阻塞调 Geocoder，见 3.4.10），故头部显示「当前位置」、城市管理显示「尚未进行定位…」；切到**百度定位**后立刻解析到**舒城县**（区县级），Baidu SDK 在 R8 下正常。这不是回归 —— 定位相关代码与 `proguard-rules.pro` 自 v3.5.3 起一行未改（3.5.3 那次冒烟显示区县名，是 MIUI 的 `allowBackup` 恢复了旧 prefs 里选中的百度定位）。

- **定位不再索要存储权限**。`BaiduLocationService`/`AMapLocationService` 的 `getPermissions()` 在 **API 29+** 只返回 `ACCESS_COARSE_LOCATION`+`ACCESS_FINE_LOCATION`，去掉 `READ_/WRITE_EXTERNAL_STORAGE`（<29 保持原样）。依据是逐个核过的、而不是猜的：`BaiduLBS_Android.jar` **238 个类里只有 3 个**碰 `Environment.getExternalStorageDirectory()` —— `e/a`（`/baidu/tempdata/` 离线定位缓存，配 SQLite）、`c/g`（`error_fs2.dat` 错误日志）、`h/k`（工具类），**定位主链路不在其中**；且**全 jar 搜不到 `*_EXTERNAL_STORAGE` 字符串**，说明 SDK 从不检查该权限、写失败就吞，裁掉不会触发它的 permission-denied 分支。各 API 段的实际净效果：**≤28** 照旧申请；**29~32** 原来会白弹一个「访问照片和媒体」框（这是唯一真实的用户可见损失，而 29+ 分区存储已封死 `getExternalStorageDirectory()` 那条路径，授了也没用）；**30+** 连 `requestLegacyExternalStorage` 都失效；**33+**（含本机 Android 14）targetSdk 35 下系统**直接忽略**该请求、不弹框——所以在 API 34 真机上新旧行为完全一致，无可观测差异，故本次未做真机对照（装 debug 需卸掉已装的签名 release、丢数据，收益为零）。消费侧安全：`LocationHelper.getPermissions` 只整数组透传 + 在 Q 上追加 `ACCESS_BACKGROUND_LOCATION`，无下标假设；`requestLocation` 的守卫查的是 `length != 0` 与 COARSE/FINE 两项，均不受影响。`assemblePubDebug` BUILD SUCCESSFUL。**顺带查出**：同批改动里给 `app/src/proprietary/AndroidManifest.xml` 的 `WRITE_EXTERNAL_STORAGE` 加的 `maxSdkVersion="28"` 是**空操作** —— 该文件根本没被合并（详见「已知问题」新增条目）。保留该属性只为将来 srcFile 一旦接上时是对的，当前不产生任何效果；**真正生效的只有 Java 侧那两处**。

- **CMA 转换器补测 + 修 build.gradle 死引用 + 接彩云预警字段**：① 新增 `CmaResultConverterTest`（10 例，Robolectric，合成固件 `src/test/resources/cma/{weather.json,hourly.html}`）。CMA WAF 拦开发机抓固件（见已知问题），故合成：weather.json 覆盖 3 天 daily/2 条 alarm/now 块、hourly.html 覆盖两张 hour-table 各 6 列（跨日 00:00 触发日递增）。锁住：cityId 来自 location、now 无天气文字按昼夜从 daily 推导、HTML 正则解析（图标号→daily code→WeatherCode 自洽映射）、跨日小时分桶、风速 m/s→km/h(×3.6)、alertList 从 data.alarm 转换、null/空入参不崩、中文文字→WeatherCode 映射（雷阵雨→THUNDERSTORM、雨夹雪→SLEET、霾→HAZE 等）。`getWeatherCode` 是 package-private 故只通过 convert() 间接验证。② `build.gradle` sourceSet 里 pub/gplay 两处 `manifest.srcFile "proprietary/AndroidManifest.xml"` 删掉——该路径缺 `src/` 前缀、文件不存在、AGP 静默跳过，是误导性死配置（AI_CONTEXT.md 已记录）。接上它需同时裁 `READ_PHONE_STATE`/`MOUNT_UNMOUNT_FILESYSTEMS`/`READ_LOGS` 三项多余权限并重新真机验证，当前不做。`foregroundServiceType=location` 已在 main manifest 的 service 声明里，不受影响。③ 彩云 `?alert=true` 不再是白请求：`CaiYunWeatherResult` 加 `AlertBean`/`AlertContent`（`result.alert.content[]`，字段 title/description/source/status/code/pubtimestamp），`CaiyunResultConverter` 加 `getAlertList()` 把每条 content 转成 `Alert`（pubtimestamp 秒→毫秒）。测试更新：固件加 1 条暴雨黄色预警、新增 `alertListIsPopulatedFromResponse`（断言 1 条预警 + description + 时间戳）和 `missingAlertYieldsEmptyList`（alert 块缺失返回空列表不崩）。`./gradlew testPubDebugUnitTest` **72 例 0 失败**（原 60，+12：CMA 10 + 彩云 2）。`assemblePubDebug` BUILD SUCCESSFUL。

- **OpenMeteo 转换器迁移到 Kotlin**（AGENTS.md「转换器先有测试再迁移」规则下的第一个）。`OpenMeteoResultConverter.java` → `.kt`（`object` + `@JvmStatic`，唯一调用方 `OpenMeteoWeatherService:48` 无需改动）。迁移过程中暴露并修掉 Java 版的两处隐患：① **null 的 weatherCode 会被塞进 @NonNull 字段** —— Java 的 `convertWeatherCode(null)` 返回 `null`，而 `Current`/`HalfDay` 的 `weatherCode` 标了 `@NonNull`（Java 侧注解不做运行时检查，所以一路静默通过），下游按非空消费（图标/配色）必 NPE；Kotlin 侧压根传不进 null，故 null 与其余未映射码走同一个 `CLEAR` 默认分支（原 `default:` 本就返回 CLEAR，没新造语义）。② **`parseDate` 缺 null 守卫** —— Java 的 `parseDate` 不判 null（姊妹方法 `parseDateTime` 判了），而 `SimpleDateFormat.parse(null)` 抛的是 NPE 不是 `ParseException`，逃到 `convert` 的外层 catch → **整个 Weather 变 null**（整次刷新失败，而非跳过一天）；Kotlin 统一收进一个 `parse()`，null 直接返回 null → 该天 `continue`。另把 day/night 两段几乎逐字重复的 `HalfDay` 构造收成 `buildHalfDay()`。测试加 1 条断言（sparse 响应无 `weather_code` 时 `getWeatherCode()` 必须是 CLEAR 而非 null）——它本身就是判别器：跑在旧 Java 版上必挂，故能证明测试确实打在新实现上。`./gradlew test` 六变体全绿、**72 例 0 失败**，`assemblePubDebug` BUILD SUCCESSFUL。

- **OWM 转换器迁移到 Kotlin，顺带修 1 个真缺陷 + 清 2 处死代码**。`OwmResultConverter.java`（563 行）→ `.kt`（`object` + `@JvmStatic`）。① **`Double.MIN_VALUE` 被当负无穷用**（真缺陷）——`buildDaily` 里 `double tempMax = Double.MIN_VALUE`，而它是最小**正**数（4.9e-324）不是最负数，全天气温为负时 `Math.max` 永远不动 → tempMax≈0。该值只在**半天无采样**时的 `(tempMax+tempMin)/2` 兜底里用到，而免费档是 3 小时步长、预报首尾两天必然是半天 → 冬季首/尾日的昼（或夜）温度会明显偏高。改用 `entries.minOf/maxOf`（不需要种子）。② **`convert(Location, OwmLocationResult, String)` 的 if/else 两支逐字节相同**（脚本核对过），`zipCode` 传进来从不读、`location` 只用于那个无意义的条件 → 收成单参 `convert(OwmLocationResult?)`，`OwmWeatherService` 两处调用同步改，顺带删掉只为传参而算的 `zipCode` 局部变量。③ **`avgHumidity` 是死代码**——累加又求平均，然后从没传进 `Daily`（`Daily` 也没有湿度字段）→ 删。另：日/夜两段几乎逐字重复的 `HalfDay` 构造收成 `buildHalfDay()`；`weatherId` 在昼分支里的那次赋值**可证明是无操作**（循环开头的无条件赋值已覆盖同一条件、同一取值），故删而非照搬；`getWindDirection` 的 16 向数组由每次调用新建改为 `private val`；`Current.airQuality` 标了 @NonNull 而 Java 侧靠构造器兜底 null、Kotlin 传不进 null → 改由 `convertAirQuality` 直接返回空 `AirQuality`（同一结果）。测试加 1 例 `subZeroDayKeepsItsRealMaximum`：把固件整体降 40℃，断言首日昼半天 = **-12**（真 midpoint）而非 **-6**（旧代码 tempMax 卡在 ~0 的结果）——夏季固件（正温度）对这个缺陷完全不敏感，故必须造负温。`./gradlew test` 六变体全绿、**73 例 0 失败**（原 72），`assemblePubDebug` BUILD SUCCESSFUL，新文件 0 编译警告。**未做真机验证**（纯重构，行为差异均有单测覆盖）。

- **WeatherAPI 转换器迁移到 Kotlin**（默认源，用户面最大）。`WeatherApiResultConverter.java`（355 行）→ `.kt`，`object` + `@JvmStatic`，唯一调用方 `WeatherApiWeatherService:45` 无需改动。本次**没查出新缺陷**（该源的历史缺陷在 3.4.13/3.4.14/3.5.4 已逐一修掉并有测试锁着），迁移只把两类「Java 注解不生效」的隐患堵上：① **`condition` 缺失时 `weatherCode` 为 null** —— `convertWeatherCode(null)` 原返回 null，而 `Current`/`HalfDay` 的 `weatherCode` 标了 `@NonNull`、Java 侧不做运行时检查，下游图标/配色按非空消费 → 一个稀疏响应就能崩，且这是**默认源**；Kotlin 传不进 null，故与未映射码走同一个 `CLEAR` 默认分支。顺带 `Current`/`Hourly` 的 weatherText 由 `condition != null ? condition.text : "Unknown"` 改成 `condition?.text ?: "Unknown"`（原写法在 condition 非空但 text 为空时仍会漏 null 进 @NonNull 字段，`HalfDay` 那条本来就是对的，现在三处一致）。② **`parseDate` 缺 null 守卫**（与 Open-Meteo 同款）——`SimpleDateFormat.parse(null)` 抛 NPE 而非 `ParseException`，会逃到外层 catch 把整个 Weather 变 null；现与 `parseDateTime` 共用一个判 null 的 `parse()`，坏日期只跳过那一天。另：日/夜两段仅差相位与温度的 `HalfDay` 构造收成 `buildHalfDay()`；`isForeignArea` 里 `location == null` 的分支是死代码（`convert` 开头 `location.cityId` 已先解引用）故删；`0xFFFFB82B` 在 Kotlin 是 Long 字面量、须 `.toInt()` 才进 `Alert` 的 int color 参数（Java 里它直接就是 int）。测试加 1 例 `missingConditionDoesNotYieldNullWeatherCode`（把 sparse 固件的 `current.condition` 置空，断言 weatherCode = CLEAR、text = "Unknown"）——跑在旧 Java 版上必挂。`./gradlew test` 六变体全绿、**74 例 0 失败**（原 73），`assemblePubDebug` BUILD SUCCESSFUL，新文件 0 编译警告。**未做真机验证**（纯重构，行为差异均有单测覆盖）。

- **彩云转换器迁移到 Kotlin**。`CaiyunResultConverter.java`（426 行）→ `.kt`，`object` + `@JvmStatic`，唯一调用方 `CaiYunWeatherService:45` 无需改动。唯一的行为变更是**修掉 `getWind` catch 分支里残留的 `new Wind(null, null, null, null)`** —— `Wind` 四个槽有三个是 `@NonNull`（`direction`/`degree`/`level`），而「Wind 构造器传 null 导致转换失败」正是 v2.6 迁移期修过的崩溃之一（见 3.3.6 回退线条目），当时只改好了 else 分支的兜底、catch 分支一直留着旧写法；Kotlin 传不进 null，故两处统一成同一个 `emptyWind()`（`Wind("", WindDegree(0f, true), null, "")`）。该分支在 `daily.wind` 数组含 null 元素时可达。测试加 1 例 `nullWindEntryDegradesToAnEmptyWind`（把 `daily.wind[0]` 置 null，断言第 0 天降级为空风、**第 1 天仍是 ENE 不受影响**）——跑在旧 Java 版上第一条断言就挂（`getDirection()` 返回 null）。其余为等价翻译：日/夜两段仅差 skycon/温度/风的 `HalfDay` 构造收成 `buildHalfDay()`；`Daily` 的 moon/moonPhase/pollen 改传 null（构造器本就兜底成同样的空对象）；`0xFFFFB82B` 在 Kotlin 是 Long 字面量须 `.toInt()`；`calendar.setTime(parseDate(...))` 那两处「靠 NPE 落到 catch」的日期兜底改成显式判空（同样的结果，但不再用异常做控制流）。**刻意保留**：`realtime` 的体感温度守卫写的是 `life_index != null` 而取的是 `apparent_temperature` —— 看着像笔误，实则 `apparent_temperature` 是**原始 double**、缺字段时读出 0.0，`life_index` 是「这份响应带全字段」的标记，去掉守卫会让缺值显示成 0°（MF 修过同类问题）；已加注释说明，以防后人「修正」成 bug。`./gradlew test` 六变体全绿、**75 例 0 失败**（原 74），`assemblePubDebug` BUILD SUCCESSFUL，新文件 0 编译警告。**未做真机验证**（纯重构，行为差异有单测覆盖）。

- **CMA 转换器迁移到 Kotlin**。`CmaResultConverter.java`（473 行）→ `.kt`，`object` + `@JvmStatic`，唯一调用方 `CmaWeatherService:115` 无需改动。**本次是纯等价翻译，没查出缺陷、也没加新测试**——该转换器的 10 例测试是 3.5.4 刚补的，覆盖已足；没有行为变更就不该造断言。实质改动只有三类：① `Current` 的 `airQuality` 原传 `null`（靠构造器兜底成空对象），Kotlin 传不进 `@NonNull`，改为直接构造同样的空 `AirQuality`（同一结果）；② 7 个正则由「每次调用 `Pattern.compile`」提为 `private val`（`HOUR_TABLE`/`TABLE_ROW`/`TABLE_CELL`/`HTML_TAG`/`ICON_NUMBER`/`HOUR_MINUTE`/`DECIMAL`），与已迁移的 `ApihzResultConverter.kt` 风格一致，逐时解析每次要跑几十次匹配；③ `0xFFFFB82B` 须 `.toInt()`。另：`getWeatherCode` 原是 **package-private**（Java 里 `static`，供同包的 `ApihzResultConverter.kt` 复用中文天气文字表，3 处调用），Kotlin 无包级可见性，取最接近原意的 `internal`（模块内），APIHZ 的 4 例测试同样全绿可证调用未断。`Matcher.group()` 在 Kotlin 下是可空返回，6 处警告用 `?: continue` / `?.let` 收紧（这些组在正则里必然参与匹配，不会真为 null，收紧只是消除警告）。`./gradlew test` 六变体全绿、**75 例 0 失败**（用例数不变），`assemblePubDebug` BUILD SUCCESSFUL，新文件 0 编译警告。**未做真机验证**（纯重构，且 CMA 的 WAF 在开发机上抓不到真实响应，见已知问题）。

- **MF 转换器迁移到 Kotlin（转换器迁移收尾）**。`MfResultConverter.java`（833 行，六源里最复杂）→ `.kt`，`object` + `@JvmStatic`，三个 `convert` 重载（坐标搜索 / 地名搜索 / 取数）与 `MfWeatherService` 的三处调用均无需改动。**又是纯等价翻译，没查出缺陷、没加新测试**——8 例测试是 3.5.3 那轮为「MF 整源不可用」写的，覆盖到 v2 载荷、UTC 时间戳、分钟级降水、预警过滤、观测回填、空 daily 判失败，一次全过。要点：① `AtmoAuraQAResult` 的 `Index.val` 字段名撞 Kotlin 关键字，须写成 `` `val` ``；② `getWeatherCode` 的 p1~p9 精确匹配串（40 多个 `equals`）收成 6 个 `private val Set`，p10+ 仍走 `startsWith`，**分支顺序原样保留**（p9 家族必须排在 `startsWith("p10")` 之前）；③ `Collections.max(list, null)` → `maxOf(a, b, c)`；④ `Alert` 首参是 `long` 而 `phenomenonId` 是 `int`，须 `.toLong()`；⑤ `forecastResult` 为 null 时，Java 是「NPE 落进外层 catch → 返回空 wrapper」，Kotlin 改成显式 `?: return wrapper(null)`（同一可观测结果，但不再拿异常做控制流）；⑥ `toInt` 保留 Java 的 `(int)(v + 0.5)` 语义而非换成 `Math.round`（对负温两者不同）；⑦ 半天聚合里 4 处 `!!` 用局部快照消掉。`./gradlew test` 六变体全绿、**75 例 0 失败**（用例数不变），`assemblePubDebug` BUILD SUCCESSFUL，新文件 0 编译警告。**至此 `weather/converters/` 只剩 `AccuResultConverter.java` 未迁**——它没有测试（Key 过期抓不到固件），按 AGENTS.md「无测试不迁」的规则不动；`CommonConverter.java` 是共享工具不在此列。**未做真机验证**（纯重构，且 MF 仅法国、本机无法端到端验证）。

- **3.5.5**：发布版（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。汇总自 3.5.4 以来：**天气转换器全面 Kotlin 化（7/8）** —— Open-Meteo、OWM、WeatherAPI、彩云、CMA、MF 六个依次迁移（APIHZ 早已是 Kotlin），只剩 `AccuResultConverter.java` 因无测试（Key 过期抓不到固件）按「无测试不迁」规则保留。迁移中查出并修掉 **3 个真缺陷**：① OWM 的 `Double.MIN_VALUE` 被当负无穷用作 tempMax 种子 → 冬季首/尾日的昼或夜温度明显偏高（免费档 3 小时步长，首尾两天必然是半天，必走那条 midpoint 兜底）；② 彩云 `getWind` 的 catch 分支残留 `new Wind(null,null,null,null)`，而 `Wind` 四槽有三个 @NonNull —— 正是 v2.6 迁移期修过的同一种崩溃，当时只补了 else 分支；③ Open-Meteo / WeatherAPI 的 `convertWeatherCode(null)` 返回 null 塞进 `Current`/`HalfDay` 的 @NonNull `weatherCode`，稀疏响应即可触发下游 NPE（WeatherAPI 还是默认源）。另清掉 OWM 的两处死代码（`convert` 的 if/else 两支逐字节相同 + 从不读的 `zipCode` 参数、算完从不使用的 `avgHumidity`）。用例数 72 → 75，`./gradlew test` 六变体全绿。**release 包真机验证（MI 9/Android 14，由 3.5.4 签名包原地升级 30504→30505）**：冷启动 0 崩溃、`logcat -b crash` 全程空；主界面下拉刷新触发真实联网（更新时刻由 18:24 变 18:28，晚于安装时刻），证明新转换器在 R8 下真实取数并落库；**「天气源可用性」实测刷新，7 个已迁移源在 R8 下全部可用** —— WeatherAPI 3天/72时、Open-Meteo 16天/384时、彩云 3天/48时、中国天气网 7天/56时、中国气象局 7天/56时（HTML 抓取正常）、OpenWeather 6天/40点、Météo France 15天/79时，体感/气压全部 ✓，各源天数点数与历史记录一致；唯一不可用的是 **AccuWeather（Key 过期，恰是未迁移的那个）**。**一处需说明**：首轮实测 OpenWeather 显示不可用，未当作「已知抖动」放过 —— 从设备直连 `api.openweathermap.org` 实测 `http=401`（无 Key 的预期响应）、TLS 1.53s 完成，证明端点可达；重跑一轮即变为可用 6天/40点，确认是已记录的国内 TLS 握手抖动而非迁移回归。

- **服务层测试先行 + Kotlin 迁移（7/8），并修掉「`cancel()` 是空操作」这个协程迁移期的行为回归**。转换器层此前已 Kotlin 化 + 75 例覆盖，而 `weather/services/` 是**唯一没被测试网罩住的核心链路**，历史缺陷恰恰密集在这层（CMA 的 COW 崩溃 3.5.2、MF 的 latch 6→5 与预警 domain 传错 3.5.3、OWM 空 daily 3.5.3、APIHZ 区→市→IP 兜底 3.4.9、CMA 站点重解析 3.4.7）。

  ① **测试基础设施**：加 `com.squareup.okhttp3:mockwebserver:4.12.0`（与已用 OkHttp 同版本），新建 `app/src/test/java/weather/services/ProviderServer.java` —— 把服务挂到 MockWebServer 上跑**真实 Retrofit/OkHttp 栈**。路由按路径前缀匹配、每条路由持有一个**响应序列**（第 1 次调用给第 1 条、末条重复），兜底链因此能一行写出「先拒绝存的 id，再回答解析出的 id」；所有请求被记录，故可断言「某调用带了某 query 参数」或「压根没发生」。固件**全部复用**转换器测试已抓的真实响应（OWM 的 current/forecast/air_pollution 与 MF 的 observation/forecast/ephemeris/rain/warning 正好对上各自扇出的端点），只新增 1 份合成的 `cma/national.json`（全国站点图；CMA 的 WAF 在开发机上抓不到真实响应，已有记录）。

  ② **37 个服务测试**（7 个类）。锁住的是**编排**而非转换：必需 vs 可选调用的区分（OWM 的 air_pollution 挂了只丢 AQI、不能拖垮整源；MF 的 ephemeris/rain/warnings 三者同时挂仍须出预报）、畸形载荷降级为 failed 而非让 Gson 异常逃出 IO 协程、以及两条历史根因 —— **MF 的预警必须串在 forecast 之后**且 `domain` 取 forecast 回的 `french_department`（测试特意把地区省份填成「Île-de-France」，这样退回 `getProvince()` 不可能凑出正确 query）、**CMA 未知站点 id 返回 `data:""` 导致重试从未触发**（断言第 2 次 view 请求必须带 `stationid=54517`、逐时抓取跟随实际命中的站点）。APIHZ 逐级验证 区→市→IP 阶梯，特别是「未知区必须先退到**市**而不是直接跳 IP」（跳 IP 会取到外省天气）。**做了变异检验**证明测试是承重的：把 `getDepartment` 改回退省份名 → 只挂那 1 条且报错直接显示 `domain=%C3%8Ele-de-France`；让 CMA 的 `tryGetWeather` 重新抛异常 → 三条依赖重试的用例挂在「expected success, but the service reported failure」（正是 3.4.7 的「无数据」症状）；APIHZ 去掉「市」这一级 → 两条阶梯用例挂（4→2、3→2）。

  ③ **查出并修掉 `cancel()` 对在途请求是空操作**（真缺陷，RxJava→Coroutines 迁移时的行为回归）。`AsyncHelper.runOnIO` 是 `launch { runnable.run() }`，体内全是阻塞 IO、**无挂起点**，`Job.cancel()` 打不断，函数体跑完照样触发回调 —— 而当年 `Disposable.dispose()` 是能拦下下游的。用 `setBodyDelay` 写测试证实（Java 版还**不确定**：`cancel()` 偶尔赶在协程被调度之前，OpenMeteo 那轮就侥幸通过了）。修法是新建 `RequestScope.kt` 承担「请求跑在哪、取消意味着什么」：**两半都要** —— 跟踪在途 `Call` 并直接 `cancel()`（否则线程一直坐在 socket 上），**且**回调前查 `isActive`（否则被取消的 Call 抛 IOException 仍会走到 `requestWeatherFailed`）。`cancel()` 取消 scope 的**子协程**而非 scope 本身，因为服务是注入一次反复使用的。顺带修掉 `mControllers` 只在 `cancel()` 里 clear、每次刷新累积 4~7 个 Controller 的泄漏 —— `execute()` 在 `finally` 里把 Call 摘掉。

  ④ **7 个服务迁到 Kotlin**：OWM、MF、APIHZ、CMA、OpenMeteo、WeatherAPI、彩云。扇出的 `CountDownLatch`+`AtomicReference`+`AtomicBoolean` 换成 `async`/`await`，MF 的「warnings 串在 forecast 之后」由**在同一个 `async` 里顺序执行**直接表达（原来靠注释说明）。只剩 `AccuWeatherService.java` 未迁（见「已知问题」）。**顺带**：CMA 的静态站点缓存 `sStations` → companion 的 `stations`（测试用反射重置，避免跨用例泄漏）；MF 的 13 个 `equals` 收成 `Set`；APIHZ 的 `normPlace`/`normSheng` 改用 `removeSuffix`。

  **验证**：`./gradlew test assemblePubDebug` BUILD SUCCESSFUL、六变体全绿，`testPubDebugUnitTest` **112 例 0 失败**（原 75，+37）。**未做真机验证** —— 取消路径的行为差异有单测覆盖，但「切换地区/退出页面时不再收到迟到回调」这条最好在下次真机冒烟时留意一下。

- **修 APIHZ 跨午夜日期错位：第 1 天按设备时钟定日期，与第 2 天撞车**。真机上肉眼发现：8-15 凌晨主界面「每日概览」出现**两列都是「今日 8-15」**，且第一列装的是 8-14 的预报（图标温度与午夜前那张截图逐项相同）。根因在 `ApihzResultConverter.convertDailyList` —— 接口的第 1 天是**平铺在根上、没有自己的日期字段**的，转换器就拿 `todayCn()`（设备当天）去锚；而第 2~7 天各自带 `date`。本地过了午夜、接口那边还没滚动时，接口的「第 1 天」仍是昨天的预报，被标成设备的今天，正好和第 2 天自带的日期撞车 → 两条都是今天，第一条是陈旧数据。**影响不止显示**：`Daily.isToday()` 会同时匹配两条，而主界面头部、全部 widget、通知都按 `dailyForecast[0]` 当「今天」读。**窗口比预想的宽** —— 真机 03:13 刷新仍复现，即午夜后数小时内持续，不是一分钟的边缘情况。改法：第 1 天的日期由**第 2 天的日期减一天**推导（`dayBefore()`，用 CN_TZ 的 `Calendar.add` 而非减 86400000，跨 DST 也对；第 2 天缺失时才退回 `todayCn()`）。顺带把第 1 天的日出日落查表也修对了（`buildAstro` 按日期查 `sunMap`，原来拿设备今天去查昨天的数据）。**扫过其余源**：CMA 的每一天都走 `parseDate(d.date)`（`CmaResultConverter.kt:136`），全部来自响应，无同款问题；其余源的 `Date()`/`currentTimeMillis()` 都是解析失败兜底或 `Base` 的 update 时刻，不是 daily 日期来源。测试加 1 例 `dayOneIsDatedFromTheResponseNotTheDeviceClock`（断言 day1=2026-08-06、day2=2026-08-07，并逐对校验相邻两天间隔恰为一天、不得重复）——**做了变异检验**：改回 `todayCn()` 后该例报 `expected:<2026-08-06> but was:<2026-08-15>`，2026-08-15 正是跑测试那天，直接坐实「按设备时钟定日期」。同时把原测试里「day 1 是时钟相关的、只能靠运气匹配固件」那条注释一并去掉（现在整条日列表都确定了）。`./gradlew test assemblePubDebug` 六变体全绿、**113 例 0 失败**（原 112）。**真机 A/B（MI 9，签名 release 原地升级）**：同一台机器同一份数据，修复前 03:13 刷新是「今日 8-15 / 今日 8-15 / 周日 8-16」，修复后 03:22 刷新变成「**周五 8-14** / **今日 8-15** / 周日 8-16」，重复消失、「今日」指向正确的一天，0 崩溃。**残留（未改）**：接口滞后时第一列会显示成昨天（如实标注，而非冒充今天）。要不要把已过去的日子从列表里剔掉是另一个设计问题 —— 全仓库 76 处 `dailyForecast[0]` 的假设会跟着变，且极端情况下整列被剔空会触发「空 daily 视为失败」，本次不动。

- **`WeatherHelper` 补测试（7 例）：锁住「空 daily 视为失败」这条全局不变量与「回调必须回主线程」**。测试网此前止步于服务层，而 `WeatherHelper` 是所有源的结果汇聚点，扛着两条下层无法自证的承诺。用 Mockito 把 `WeatherServiceSet` 换成假的（它是普通 Java 类、`get()` 可打桩），服务用一个继承 `WeatherService` 的 `FakeService` 从 IO 线程回调，与真实服务同构。覆盖：空 daily → 判失败且**旧缓存必须保留**、正常响应 → 成功且写穿缓存、源直接失败 → 同样保留缓存、成功/失败回调都必须落在主线程、多源搜索取并集、单源抛异常不拖垮其余、无启用源直接失败。**踩到的三个坑**：① Robolectric 下测试**跑在主线程**，而回调经 `Dispatchers.Main` 投递 —— 直接 `latch.await()` 会和主 looper 死锁，改成「边 `shadowOf(mainLooper).idle()` 边轮询」；② Room 拒绝主线程访问（正是仓库那条约束），测试里的 DB 读写必须自己开线程 join；③ **`DatabaseHelper.sInstance` 与 `GeometricWeatherDatabase.sInstance` 是进程单例，而 Robolectric 在方法之间重建应用数据目录** → 第二个用例拿着指向已删除文件的连接，**直接掀翻测试 JVM**（每条单独跑都过、合起来跑崩，且无 hs_err，极具迷惑性）；在 `setUp` 开头反射 close + 置空两个单例解决。**变异检验的意外收获**：去掉 `WeatherHelper:57` 的空 daily 守卫后，测试不是断言失败而是**整个 JVM 崩溃** —— 追下去是 `HistoryEntityGenerator.java:28-29` 在 `writeWeather` 链路里无保护地 `getDailyForecast().get(0)`。也就是说这条守卫护住的首先是**持久化层的硬崩**，比文档里记的「UI/widget 层 76 处 get(0)」更靠前；`DatabaseHelper` 读路径有空 daily 判断而写路径没有，这处不对称**本次未改**（既有设计是由 `WeatherHelper` 与 `readWeather` 两个汇聚点担保，AI_CONTEXT 也已写明「绕过这两处直接构造 Weather 的新代码必须自己判空」），仅在此记录。`./gradlew test assemblePubDebug` 六变体全绿、**120 例 0 失败**（原 113）。**未做**：`LocationHelper` 的测试 —— 它的 `@Inject` 构造器收的是**具体**定位服务类型（AMap/Baidu/Android），塞不进假实现，需 mock 具体类或反射改 `mLocationServices` 数组，是另一块工作量。

- **`DatabaseHelper` 写路径补空 daily 守卫（把进程级崩溃降级为不写 history 行）**。上一条的变异检验暴露出来的：`HistoryEntityGenerator.generate(cityId, source, weather)` 里 `getDailyForecast().get(0)` 无保护，而它由 `DatabaseHelper.writeWeather` 调用 → 一个空 daily 的 Weather 只要绕过 `WeatherHelper` 那道守卫落到写库，就不是优雅降级而是 `IndexOutOfBoundsException` **掀翻整个进程**（测试里表现为 JVM 直接退出、连 hs_err 都没有）。history 行记的是当天昼夜温度，只有 daily[0] 能提供 —— 没有 daily 就是没有 history 可记，故 `generate` 改为返回 `@Nullable`、空 daily 时返回 null，`writeWeather` 判空后跳过插入（其余 daily/hourly/minutely/alert 的批量插入本就能吃空列表）。这是**第二道**而非第一道防线：`WeatherHelper` 仍然拦在前面，加这一道只因为此处的失败是致命的而非可恢复的。新增 `app/src/test/java/db/DatabaseHelperTest.java`（2 例：正常 Weather 往返存取、空 daily 写入不得崩）。**变异检验**：去掉守卫后该例干净失败并报出 `IndexOutOfBoundsException: Index 0 out of bounds for length 0`（而不再是掀翻 JVM——异常被测试线程接住了）。`./gradlew test` 六变体全绿、**122 例 0 失败**（原 120）。**读写不对称至此消除**：`readWeather` 早已对空 daily 行返回 null，现在 `writeWeather` 也不会因空 daily 崩了。

- **3.5.6**：发布版（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。汇总自 3.5.5 以来的四件事：① **`weather/services` 迁移到 Kotlin（7/8）+ 37 例服务编排测试**（加 mockwebserver，复放已有真实固件），其中**修掉 `cancel()` 对在途请求是空操作** —— RxJava→Coroutines 迁移时的行为回归，`launch { runnable.run() }` 体内全是阻塞 IO、无挂起点，`Job.cancel()` 打不断，回调照常送达；新建 `RequestScope` 同时取消在途 `Call` 并在回调前查 `isActive`，用户侧表现是切换地区/退出页面后不再收到迟到回调。② **修 APIHZ 跨午夜日期错位** —— 接口第 1 天平铺在根上无日期字段，转换器拿设备当天锚定，与第 2 天自带日期撞车 → 午夜后数小时内「每日概览」出现两个「今日」，且第一列是昨天的预报；主界面头部/全部 widget/通知都按 `dailyForecast[0]` 当今天读，一并受影响。改为由第 2 天日期减一天推导。③ **`WeatherHelper` 补测试 7 例** —— 锁住「空 daily 视为失败并保留旧缓存」与「回调必须回主线程」。④ **`DatabaseHelper` 写路径补空 daily 守卫**（见上条）。用例数 75 → 122。顺带修正 AI_CONTEXT 顶部「当前发布版本」——3.5.5 发版时漏更，一直停在 3.5.4。

- **`LocationHelper` 补测试（5 例）+ 修「地区可用时失败回调不回主线程」**。定位分两段：定位服务给坐标（百度/高德还给地址），再由当前天气源把坐标解析成它能供数的地区。① **可测性重构**（机械、行为不变）：原构造器**在内部 `new` 出 4 个定位服务**（`AndroidLocationService`/`BaiduLocationService`/`AMapLocationService`），塞不进假实现、连实例化都会拉起百度 SDK；改为主构造器委托给一个 `@VisibleForTesting` 的构造器接收 `LocationService[]`，Hilt 那条路径原样不变。② **查出并修掉线程不一致**：`requestLocation` 有三条返回路径，成功与北京兜底都走了 `delayRunOnUI` 回主线程，唯独「地区已可用但解析失败」那条是**直接同步回调**，而它恰恰是从天气服务的 IO 线程进来的 —— 与 3.4.x 修过的「搜索地点闪退」同一形状（当时也是漏了回主线程）。今天没崩是因为 `MainActivityRepository` 把它转交给了 `WeatherHelper`（后者有回主线程保证），属巧合而非设计；已补上 `delayRunOnUI`，三条路径统一。**测试是先写后修的**：`callbacksArriveOnTheMainThread` 一开始就挂在这条，修完转绿。③ 5 例覆盖：坐标→源解析→落库并上报、**北京兜底**（定位失败且原地区不可用 → 采用默认地区 101924 并持久化，这是 3.3.7–3.3.13 因定位回归被整体回退后**刻意保留**的行为，故钉住防止被"清理"）、原地区可用时定位失败不得被兜底覆盖、源解析不出地区算失败而非崩溃、回调必须回主线程。`./gradlew test assemblePubRelease` 六变体全绿、**127 例 0 失败**（原 122）。**真机验证**（MI 9，签名 release 原地升级）：百度定位仍解析到**舒城县**（区县级）、联网取数正常、0 崩溃 —— 定位是本项目唯一因回归被整体回退过的区域，故此项不可跳过。

- **ACCU 在天气源选择页标注为不可用**。内置 Key 自 2026-06-28 起过期，而源列表里它和其余源看起来毫无区别 —— 选中后只会一直「获取天气数据失败」，界面上没有任何解释。**没有移除也没有禁用**：`SettingsManager.providerAccuWeatherKey` 是 `customValue.ifEmpty { defaultValue }`，用户在「高级 → 设置服务商 API KEY」填了自己的 Key 就能正常用，砍掉会误伤这条路径。改为在名称后追加标注，且**仅当 `customAccuWeatherKey` 为空**（即真的在用已失效的内置 Key）时显示，填了 Key 就自动消失。实现：`ServiceProviderSettingsScreen` 的天气源项由 `ListPreferenceView(nameArrayId=…)` 改为传显式 `nameArray`（与同页位置服务按 flavor 过滤的写法一致），按 `weather_source_values` 里的 `accu` 定位下标；新增字符串 `settings_weather_source_needs_own_key`（带 `%1$s` 格式参数，中英文各自控制括号与空格）。**真机验证**（MI 9，签名 release）：弹窗里显示「Accu Weather（不可用 —— 内置密钥已失效，需自填）」，选中后折叠行同样完整显示未被截断，其余 7 个源不受影响；切回中国天气网后正常取数、0 崩溃。`./gradlew test` 六变体全绿、127 例 0 失败。

- **3.5.7**：**正式版**（`assemblePubRelease` 已签名 + R8 minify/shrinkResources；此前 3.5.2–3.5.6 均为 Prerelease）。汇总自 3.5.6 以来的两件事：① **`LocationHelper` 补测试 5 例 + 修「地区可用时失败回调不回主线程」** —— `requestLocation` 三条返回路径里，成功与北京兜底都回了主线程，唯独这条从天气服务 IO 线程进来的直接同步回调，与 3.4.x 修过的「搜索地点闪退」同一形状；顺带做了可测性重构（4 个定位服务由构造器内 `new` 改为 `@VisibleForTesting` 构造器注入，Hilt 路径不变）。② **天气源选择页标注 AccuWeather 不可用** —— 内置 Key 已过期而列表里它与其余源毫无区别，选中后只会一直失败且无任何解释；改为追加「不可用 —— 内置密钥已失效，需自填」，仅在 `customAccuWeatherKey` 为空时显示，用户填了自己的 Key 即消失（该源在自带 Key 下可正常使用，故不移除）。用例数 122 → 127。**release 包真机验证（MI 9/Android 14，按发版流程卸载后全新安装，全新数据）**：冷启动 0 崩溃、`logcat -b crash` 全程空；新装默认源确为 WeatherAPI 并联网拉取成功（23°、体感 27°、空气质量 优、日期「今日 8-15 / 周日 8-16 / 周一 8-17」正确、夜间配色正确）；天气源弹窗中 AccuWeather 显示「不可用 —— 内置密钥已失效，需自填」且换行完整可读，其余 7 个源无标注；切百度定位后解析到**舒城县**（区县级），刷新正常 —— 定位代码本版有改动，此项不可跳过。

- **移除「高级 → 设置服务商 API KEY」设置项**。按用户要求删掉该入口：`ServiceProviderSettingsScreen` 的整个「高级」分组（含分组头尾）、路由 `SettingsScreenRouter.ServiceProviderAdvanced`、两处 NavHost 注册（`SettingsActivity` / `SelectProviderActivity`）与页面文件 `ServiceProviderAdvancedSettingsScreen.kt` 一并删除。**没有动 `SettingsManager` 的 `customAccu*Key`/`customOwmKey`/`customMfWsftKey`/`customBaiduIpLocationAk`/`customWeatherApiKey` 等属性** —— `providerXxxKey` 仍是 `customValue.ifEmpty { defaultValue }`，老用户此前存进 SharedPreferences 的 Key 继续生效，只是应用内不再有改动入口；自填 Key 现在只能走 `local.properties` / `build.gradle`。顺带把 ACCU 那条标注的措辞从「不可用 —— 内置密钥已失效，需自填」改成「不可用 —— 内置密钥已失效」（中英文各一处）——「需自填」指向的页面已经不存在了。判定条件仍是 `customAccuWeatherKey.isEmpty()`（老用户存过 Key 时标注照旧消失），未改成无条件标注。**未删**：`settings_title/summary_service_provider_advanced`、`settings_category_advanced`、`settings_provider_*` 等字符串（散在 10 个语种文件里，留着不影响构建），以及 `EditTextPreferenceView`/`editTextPreferenceItem`（属通用 preference 组件，此后暂无调用方）。`./gradlew :app:assembleFdroidDebug` 通过。


- **新增「综合」天气源（COMPOSITE）：并行拉多个源、按块合并成一份数据**。现有每个源都缺一块 —— Open-Meteo 最长（16 天 / 384 小时，带 UV）但**完全没有 AQI 和预警**；WeatherAPI 短一些（14 天 / 336 小时）但两样都有。新源 `CompositeWeatherService` 同时问这两家，用 `WeatherMerger` 把答案折成一份。**合并规则是「按块」不是「按字段」，这是整件事的核心**：排在前面的源（Open-Meteo）当骨架，它的每一天/每一小时**整条**保留 —— 温度、天气现象、降水、风必须来自同一个源，混着取的后果是渲染出「晴天 + 5mm 降水」这种比任何单一源都差的结果。只有**与预报叙事无关**的块才从后面的源补，且仅在骨架那块为空时：AQI、UV、花粉、日出日落、月相、日照时长（各自复用模型已有的 `isValid()`，没有自己造判空）；预警取并集去重；分钟级降水取第一个非空的整段。骨架覆盖不到的日期/小时**整条追加**，所以序列长度取所有源里最长的那个。日按 `yyyyMMdd`（设备时区，因为两个转换器目前都把日期解析进设备时区）对齐，小时按绝对小时桶（`time / 3600000`）对齐。实现要点：① 复用 `WeatherService` 层而不是直接调 API —— 想加第三个源只改 `CompositeWeatherService.sources` 一行，不用重写请求参数；② 用 `async/awaitAll` + `suspendCancellableCoroutine` + `withTimeoutOrNull(30s)` 而不是 `CountDownLatch`，取消能沿着 `RequestScope.cancelChildren` 一路传到委托源的 `cancel()`，不留 30 秒僵尸线程；③ 委托实例是 Composite 自己 `@Inject` 的，**不是** `WeatherServiceSet` 那批，所以 `cancel()` 必须自己往下转发。**注册了 5 处**（无需 `ApiModule`/`build.gradle`，不新增 Retrofit 与 Key）：枚举**追加在末尾**（`Location` 按 ordinal 做 Parcel）+ `getInstance()` 加 `contains("composite")` 分支（不加会被 fallback 成 ACCU，Room 存的是 id 字符串）、`WeatherServiceSet` 三处、`values/` 三个数组 + `values-zh-rCN/` 两个数组（**排在第一位**，展示顺序按「好用的在前」）、`WeatherSourceStatusScreen` 加一行（可在真机上和各单源横向对比，是本功能的验收工具）。新增 `WeatherMergerTest.kt` 7 例，直接跑**两份真实固件**（都覆盖 2026-08-11..13，互补得刚好：Open-Meteo 72 小时无 AQI 无预警，WeatherAPI 18 小时有 AQI 有预警）：不得跨源混字段（并带一条「两份固件必须真的不一样」的反空转断言）、AQI 从有的那家补、预警并集、骨架有数据时不被覆盖、序列取最长、重叠小时不重复、单源/零源退化。**变异检验**：把 `fillDaily` 的 `leader.day()` 改成 `others.firstOrNull()?.day()`，`theLeadersOwnReadingsAreNeverBlendedWithAnothers` 与 `theSeriesRunsAsLongAsTheLongestProvider` 双双失败，守卫是实的。**代价与未做**：每次刷新多一次网络往返（这是「更全」的对价）；**默认源仍是 WEATHERAPI 未改**（改默认会让所有新装用户的流量翻倍，属另一个决定）；只混了 2 家 —— 加中国源（CMA/APIHZ）意义不大，天气文案来自骨架，它们能补的 AQI/UV 恰好都没有。

- **修「12 个语种的天气源数组停在上游 4 项旧顺序、标签与实际 id 整体错位」**。上一条把 `values/` 的天气源数组加到 9 项时发现的既有 bug：`values-fr|ja|ko|ru|cs|es|fi|pl|pt-rBR|ro|el|zh-rHK|zh-rTW` 里 `weather_sources` 只有 4 项、且是上游的老顺序（caiyun/accu/owm/mf），而 `weather_source_values`（真正的 id 表）只存在于 `values/`。两张表按下标配对，于是这些语种下**法语用户看到的「AccuWeather」其实是 `open_meteo`**，且只能选到前 4 个源。不崩是因为 `Utils.getNameByValue` 与 `ListPreferenceView` 都用 `zip`（悄悄截断到 4 项）。**改法是删而不是补**：11 个非中文语种的这两个数组**整个删掉**，让 Android 的资源回退机制去拿 `values/` 的 9 项英文名 —— 源名本来就大多是专有名词（WeatherAPI / Open-Meteo / OpenWeather / Météo France），损失只有「（仅中国地区）」这类括号注释，而**补 9 项到 13 个文件等于把同样的漂移隐患原样重建一遍**。`zh-rHK`/`zh-rTW` 例外：回退成英文会丢掉「中國天氣網 / 中國氣象局」，故按 `zh-rCN` 补成正确的 9 项繁体。**顺带堵掉崩溃本身**（治本）：`ListPreferenceView` 与 `ServiceProviderSettingsScreen` 里那句 `names[values.indexOfFirst{…}]` 是硬下标，数组再漂移或存了个已不存在的 id（`indexOfFirst` 返回 -1）就会越界；改成 `getOrNull(...) ?: value`，最差显示原始 id 而不是掀掉设置页。净 -98/+31 行。

- **修 Open-Meteo `past_days=1` 把昨天顶到 `dailyForecast[0]` / `hourlyForecast[0]`（OPEN_METEO 与 COMPOSITE 全天错一天）**。3.5.8 给综合源做真机验收时肉眼发现：主界面「每日概览」第一列是**周一 8-17**（昨天）、「小时概览」第一格是 **0时 8-17**，整整 24 小时的过去数据排在最前。根因不在合并器，在 `OpenMeteoWeatherService` 的请求参数：`PAST_DAYS = 1`，注释写着「Yesterday, for the day-over-day comparison on the main screen」，但 `OpenMeteoResultConverter` 从来没把那个过去日剥出去 —— `Weather` 的 `yesterday` 槽传的是 `null`（那一槽实际由 `DatabaseHelper.readWeather` 从 history 表填，见 `WeatherDatabaseDao:118` 的 `date >= :yesterday AND date < :today`），而昨天那条连同它的 24 小时原样进了 `dailyForecast`/`hourlyForecast` 的**头部**，于是全仓库 76 处把 index 0 当「今天/现在」读的代码全都错一天。**比 3.5.6 修的 APIHZ 那个更严重**：APIHZ 只在午夜后数小时的窗口内错，这个是**全天候错**。**它还污染了 history 表**：`HistoryEntityGenerator.generate(cityId, source, weather)` 用 `base.publishDate`（今天）当日期、却取 `dailyForecast[0]`（昨天）的昼夜温度 —— 也就是说 `past_days=1` 不但没实现注释里声称的「昨日对比」，反而把该功能真正的数据源写坏了一天。**改法是不要请求它**（`PAST_DAYS 1 → 0`）而不是在转换器里过滤：不想要的数据就别拿，一处请求参数比一处过滤 + 一处注释更少。同时 `FORECAST_DAYS 15 → 16` —— 实测 `past_days=0&forecast_days=16` 恰好还是 **16 天 / 384 小时**（`2026-08-18 → 2026-09-02`），视野一点没少，只是全部落在今天及以后（原先的 16 天 = 1 天过去 + 15 天未来，384 小时 = 24 小时过去 + 360 小时未来），故「各源数据丰富度」里 OPEN_METEO 那句 16天·384时 无需改。**全仓库 grep `past_days` 只有 Open-Meteo 一家**，其余源不受影响；但 COMPOSITE 拿 Open-Meteo 当骨架，所以它跟着一起修好。测试加 1 例 `noPastDaysAreEverRequested`，用已有的 `ProviderServer.requestedPath` 断请求串里的 `past_days=0` 与 `forecast_days=16` —— 守的是「请求参数」这个根因，而不是某份固件的形状（固件是静态的，永远不会自己长出过去日）。**变异检验**：改回 `past_days=1` 该例 FAILED，改回 0 通过。`./gradlew test assemblePubRelease` 六变体全绿、**135 例 0 失败**（原 134）。**真机 A/B（MI 9，签名 release 原地升级）**：同一台机器同一个源，修复前是「每日概览 周一 8-17 / 今日 8-18 …」+「小时概览 0时 8-17」，修复后是「**今日 8-18** / 周三 8-19 …」+「**0时 8-18**」，0 崩溃。

- **3.5.8**：Prerelease（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。汇总自 3.5.7 以来的三件事：① **新增「综合」天气源**（Open-Meteo 当骨架 + WeatherAPI 补 AQI/预警，按块合并，见上文）；② **移除「高级 → 设置服务商 API KEY」入口**；③ **修 Open-Meteo `past_days=1` 把昨天顶到 index 0**（见上条，是验收综合源时查出来的既有缺陷）。用例数 127 → 135。**release 包真机验证（MI 9/Android 14，由 3.5.7 签名包原地升级 30507→30508，保留原数据 —— 全新安装路径已在 3.5.7 验过，这次不清用户数据）**：冷启动 0 崩溃、`logcat -b crash` 全程空；下拉刷新触发真实联网（更新时刻 01:03，晚于 01:02 的安装时刻），证明综合源的双路并发请求在 R8 下真实取数并落库；「每日概览」首列 今日 8-18、「小时概览」首格 0时 8-18（过去日不再顶头）；AQI 84 良、PM2.5 62.2 / PM10 63.1 / SO₂ 6.2 / NO₂ 46.3 / O₃ 50 五项齐全 —— Open-Meteo 一家完全没有 AQI，这些全部来自 WeatherAPI，即合并真的生效；百度定位解析到**舒城县**（区县级）；「数据提供商」页确认「高级」分组已消失。**「天气源可用性」实测（01:12，北京坐标）横向对比是本次最直接的验收证据**：综合 = 16 天 / 384 时 + AQI ✓ + 预警 4 条；Open-Meteo 单独 = 16 天 / 384 时但 **AQI ✗ / 预警 0**；WeatherAPI 单独 = **只有 3 天 / 72 时**但 AQI ✓ / 预警 4 —— 综合把 WeatherAPI 的 3 天视野拉到 16 天、同时保住了它独有的 AQI 与预警，正是设计意图。其余源照常（彩云 3天/48时、中国天气网 7天/56时、中国气象局 7天/56时、OpenWeather 5天/40点、Météo France 15天/73时），AccuWeather 仍「不可用」（内置 Key 过期）。**顺带修正一处陈旧记录**：「各源数据丰富度实测」里 WEATHERAPI 写的是 14天·336时（2026-06-28 的数），当前 Key 实测只给 **3 天 / 72 时**（免费档，3.5.5 的实测里已是这个数但没回写到那行）—— 这反而让综合源比原描述更值：原文说 WeatherAPI「短一些」，实际是短很多。

- **修「彩云源把全国区县整体塌回地级市」（89% 的城市查错）+ 顺带修城市表读取的编码依赖**。用户报告：切到彩云后主界面标题是**六安**而不是**舒城**。彩云与坐标型源不同 —— 它**不保留**百度反查出来的地名，而是拿省/市/区去查内置的 3216 条中国城市表（`assets/city_list.txt` → Room `chinese_city`），**用查出来的那一行整体替换当前位置，连坐标一起换**（`ChineseCity.toLocation()`，`ChineseCity.kt:14`）。表里明明有舒城（`101221507 / 31.462849, 116.94409`，正是用户所在坐标），所以不是数据缺失。**根因在 `WeatherDatabaseDao.selectChineseCityByRegion` 那句 SQL**：8 个条件的 OR 阶梯，**既没括号也没 `ORDER BY`**。SQL 里 `AND` 比 `OR` 结合更紧，阶梯中间那条于是退化成裸的 `city = :city`；而 `LIMIT 1` 在无排序时，SQLite 返回的是**表里物理顺序最靠前**的那条满足任一条件的行，而不是「最先匹配的那个条件对应的行」—— `city_list.txt` 里地级市自己那行永远排在它下属区县之前（安徽/六安/六安 是第 2106 条，舒城是第 2111 条），于是弱条件「市名相等」压过了强条件「区县+市都相等」。**拿真实 asset 离线建表全量扫过一遍量化：2855 / 3216 条（89%）解析到错误的城市** —— 不是舒城特例，而是全国区县在彩云下普遍塌成地级市（天津/南开 → 天津，安徽/金安 → 六安）。**后果不止标题**：替换上来的坐标是六安市区的 `31.75/116.51`，离舒城约 50 km，取到的是那边的天气。**改法**：补一段 `ORDER BY`，把 WHERE 里那条阶梯**照原序**变成排序键（WHERE 一字不改、不加 DAO 方法、不碰 schema，所有调用方一起生效）；同一份离线全表扫在改后是 **0 条**出错。**写测试时撞出第二个缺陷**：真机上能查到，测试里连 `readChineseCity("舒城")` 和 `LIKE '%舒城%'` 都返回空，**只有坐标扫描能命中** —— `FileUtils.readAssetFileToString` 读 asset 没指定字符集，用的是 JVM 平台默认（这台 Windows 是 GBK），427 KB 的 UTF-8 城市表整张解成乱码；经纬度是 ASCII 所以坐标查询照常工作，中文列全废，症状极具误导性。**Android 的默认字符集恒为 UTF-8，所以线上从未受影响**，属单测环境暴露出的潜在宿主依赖；显式写上 `StandardCharsets.UTF_8`。新增 `DatabaseHelperTest.aDistrictResolvesToTheDistrictAndNotItsPrefecture`（真实 asset 全量入库后断言 舒城/南开/南山 各自解析到自己、区县为空时才退回地级市，并带一条「表必须真的加载了 3216 条」的反空转断言）；顺带把该类的 `offMainThread` 从「静默超时 + 静默吞异常」改成会报出来 —— 原来 worker 线程死掉只表现为结果全 null，看着像逻辑错而不是崩溃，正是这次排查绕远路的原因。**变异检验**：去掉 `ORDER BY` 后该例报 `expected:<10122150[7]> but was:<10122150[1]>`，正好是舒城 vs 六安。`./gradlew test assemblePubRelease` 六变体全绿、**136 例 0 失败**（原 135）。**真机验证**（MI 9，签名 release 原地升级，天气源保持用户当前选的彩云）：主界面标题由「六安」变「**舒城**」，地区管理页显示「中国 安徽 六安 舒城 / Powered by caiyunapp.com」，34°、晴、空气质量 优、3 天预报（试用 Token 上限），`logcat -b crash` 全程空。**未改**：`selectChineseCityByName`（`district = :name OR city = :name LIMIT 1`）是同一形状的隐患（搜「朝阳」时北京朝阳区与辽宁朝阳市谁在表里靠前谁赢），但 `readChineseCity(String)` 全仓库没有调用方，是死代码。

- **删掉同形状的死查询 `selectChineseCityByName`**（上一条的收尾）。`SELECT * FROM chinese_city WHERE district = :name OR city = :name LIMIT 1` 与刚修的区县查询是同一个坑：无排序的 OR + `LIMIT 1`，搜「朝阳」时北京朝阳区和辽宁朝阳市谁在表里靠前谁赢。**没有给它补 `ORDER BY`，而是连同唯一的包装方法 `DatabaseHelper.readChineseCity(String)` 一起删掉** —— 全仓库（含 amap/noamap/bugly/nobugly/opensource/proprietary 六个 flavor 目录）没有任何调用方，而地名搜索走的是另一条真正在用的 `readChineseCityList(name)`（`LIKE` 查询、返回列表、由彩云的搜索页调用）。给死代码补正确性等于给它续命。净 -9 行，六变体 136 例 0 失败。

- **「综合」源改成按块指派来源：小时 Open-Meteo / 每日 中国天气网 / 空气质量与详情 彩云**（按用户要求）。原来的设计是「一个骨架 + 补空块」——`results[0]` 同时领导所有块，只有它留空的地方才从别人那里补。这做不到「小时来自 A、每日来自 B」，所以 `WeatherMerger.merge` 从「一个 leader」改成**每块各自一份优先级列表**（`daily` / `hourly` / `current` / `airQuality` 四个参数，默认值为 `results`，故旧调用语义不变）。**块内规则一字未改**：每条日/时仍然整条来自该块的 leader（温度、天气现象、降水、风必须同源），只有与预报叙事无关的读数（AQI、UV、花粉、日出日落、月相、日照时长）才从别人那里补；leader 覆盖不到的日期/小时仍整条追加。一处**行为变化**：AQI 现在**优先服从自己的指派**，而不是「leader 有就用 leader 的」——这正是「空气质量用彩云」的要求。成员从 2 个变 4 个（加 APIHZ、彩云）；**WeatherAPI 保留未删** —— 用户没提预警，而它是唯一稳定出预警的源（设置里「推送预警通知」是开着的），删掉等于静默丢功能，现在它的角色是预警 + 兜底。**优先级不是硬绑定**：某源失败/超时/该块没数据就落到下一家，所以境外（APIHZ 与彩云都无话可说）仍能从 Open-Meteo + WeatherAPI 拿到完整预报。顺带更新了源名标注（`values/` 与 `zh-rCN/rHK/rTW` 四处，现为「综合（小时 Open-Meteo · 每日 中国天气网 · 空气 彩云）」）与 `WeatherSource.COMPOSITE` 的署名串（`open-meteo.com + apihz.cn + caiyunapp.com`）。测试加 2 例：`eachBlockFollowsItsOwnLeader`（把 daily/hourly/current 指给不同的源，逐项断言各自跟随自己的 leader —— 小时那条特意在**两家都覆盖的那个小时桶**上比较，因为并集长度 72 无论谁领导都一样，长度证明不了领导权；并对每处比较都带「两份固件在这一项上必须真的不同」的反空转断言，其中当前温度 29.7 与 29.1 取整后都是 29，故改用天气现象文本与体感）、`airQualityFollowsItsAssignmentEvenWhenTheLeaderHasOneOfItsOwn`（把 WeatherAPI 固件的 PM2.5 改成 199 造出第二个有 AQI 的源，断言 AQI 服从指派而其余块仍归各自 leader）。**变异检验**：让 daily/hourly 忽略自己的指派（改回用 `results`），两条新用例双双 FAILED。六变体全绿、**138 例 0 失败**（原 136）。**真机验证（MI 9，签名 release 原地升级，逐条对上四项指派）**：当前天气变成中文「多云, 体感 32°」（原来是 Open-Meteo 的英文 "Partly cloudy"）→ current 来自彩云；AQI 由 WeatherAPI 那份的「PM2.5 62.2 / PM10 63.1、无 CO」变成「38 优 / PM2.5 23 / PM10 38 / SO₂ 7 / NO₂ 19 / O₃ 117 / **CO 0.6**」→ 空气质量来自彩云；详情数据里「实时 E 1.2 米/秒（一级）」「气压 1006.35 毫巴」「能见度 13.57 千米」「云量 9%」是彩云的字段与单位，而「白天/夜间 东风 0 米/秒（3~4级）」的风级写法是中国天气网的 → 每日来自 APIHZ；每日列表 8-18 → 9-2 共 **16 天**（中国天气网 7 天 + Open-Meteo 追加 9 天，视野没丢）；署名行显示 `Powered by open-meteo.com + apihz.cn + caiyunapp.com`；`logcat -b crash` 全程空。**代价与残留**：每次刷新 4 个并发请求（原 2 个）；第 8 天以后的日期来自 Open-Meteo，其天气文案是英文，与前 7 天的中文并排，属既有的「追加整条」规则的可见后果，本次未处理。

- **多源聚合改名 + 每块标注数据来源 + 小时概览锁到当前时间 + 修「每日改由中国天气网领导后，昨天又回到 index 0」**（按用户要求的三件事，第四件是真机验收时抓出来的）。① **改名**：源名由「综合（小时 Open-Meteo · 每日 中国天气网 · 空气 彩云）」改为 **「多源聚合」**（`values/` 英文 `Multi-source` + `zh-rCN/rHK/rTW`，`weather_sources` 与 `weather_source_voices` 两个数组各一处）—— 括号里那串说明搬到了各卡片标注上，选择页不必再挤。② **每块标注来源**：五张卡片标题后追加「· 来源」（每日概览 · 中国天气网 / 小时概览 · Open-Meteo / 空气质量 · 彩云天气 / 日月升落 · 中国天气网 / 详情数据 · 彩云天气）。新增 `CompositeBlock` 枚举**同时**充当服务端的取数计划与界面上的标注来源 —— 这是关键设计点：两张表会漂移，一张不会，而**标注错的来源比不标更糟**。仅在源为多源聚合时显示（单一源下每张卡片都会写同一个名字，页脚署名已经说了）。标签文字取 `WeatherSource.getVoice()`（短名，如「彩云天气」而非「彩云天气（仅中国地区）」），格式串 `composite_block_credit` = `%1$s · %2$s`。**未做**：标注写的是**指派**而非实际来源 —— 若彩云当次请求失败、AQI 落到 WeatherAPI 兜底，标签仍显示彩云；要做到「标注真实来源」得把逐块来源随 Weather 存进库，而 schema 锁在 v63、冷启动读缓存也拿不到，代价远大于收益。③ **小时概览锁到当前时间**：所有源都按整天返回，于是晚上 11 点打开时小时列表仍从 0 时开始，一整排是已经过去的小时。新增 `Weather.withHoursFrom(now)` 丢掉已过去的小时（**保留正在进行的那一小时** —— 判据是 `hour + 1h > now` 而非 `hour >= now`），在 `HourlyViewHolder` 卡片入口裁一次即可覆盖整张卡片（趋势适配器都从传进来的 `location` 读数据，故 `Location.copy(location, trimmed)` 一行搞定，标签页也跟着走）。裁完为空（缓存太旧）则原样返回 —— 宁可显示旧数据也不给空卡片。④ **真机验收时抓出的回归**：01:19 打开发现「每日概览」第一列是**周二 8-18（昨天）**。原因是每日改由中国天气网领导，而它在午夜后数小时仍把昨天当第 1 天（3.5.6 已记录的接口滞后，当时的修复是让它**如实标注**而非冒充今天），于是昨天回到了 `dailyForecast[0]` —— 与前一天刚给 Open-Meteo 修掉的 `past_days` 问题同类，换了个入口复发，而全仓库 76 处把 index 0 当今天读。新增 `Weather.withDaysFrom(now)`（按设备时区取今天 0 点为界）并在 `CompositeWeatherService` 合并后调用一次；同样保留「全丢则不丢」的兜底。测试新增 6 例：`WeatherTrimTest` 5 例（小时：从当前这一小时开始、整点边界上一小时算已过去、无可丢/全可丢都原样返回；日：从今天开始、无可丢/全可丢原样返回）+ `CompositeBlockTest` 3 例（指派表内容、多源聚合下标题带来源、单一源下标题不动）——共 864 → **876 例 0 失败**（146 例/变体）。**变异检验**：把「当前这一小时」判成过去 → 小时三例全红；把日判据改成永不丢弃 → `theDayListStartsToday` 红。**真机验证（MI 9，签名 release 3.5.9）**：11:37 时「小时概览 · Open-Meteo」首格为 **11时**（原来固定 0 时）；「每日概览 · 中国天气网」首列为 **今日 8-19**（修复前是 周二 8-18）；空气质量 48 优带 CO（彩云）、详情数据 NE 2.1 米/秒 + 云量 51% + 能见度 9.28 千米（彩云）、日月升落 05:40/18:52（中国天气网）；小时卡副标题是彩云的中文文案「阴，今天下午14点钟后转小雨，其后多云」；天气源弹窗显示「多源聚合」且为选中态；`logcat -b crash` 全程空。

- **3.5.9**：**正式版**（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。汇总自 3.5.8 以来的四件事：① **修 Open-Meteo `past_days=1` 把昨天顶到 index 0**（全天候错，且污染 history 表）；② **修彩云源把全国区县整体塌回地级市**（3216 条里 2855 条查错，根因是无排序的 OR + `LIMIT 1`；顺带修掉城市表读取依赖 JVM 平台字符集）；③ **删掉同形状的死查询 `selectChineseCityByName`**；④ **多源聚合改名 + 每块标注来源 + 小时概览锁到当前时间 + 修每日 index 0 回到昨天**（见上条）。用例数 135 → 876（六变体合计，146 例/变体）。**release 包真机验证**见上条，0 崩溃。

- **「详情数据」卡改成小卡片网格（两列，带量规）**。按用户给的参考图重排，但**用 App 自己的配色和圆角**而不是参考图那套蓝色：小卡背景取 `DisplayUtils.getWidgetSurfaceColor`（与标签 chip 同一个抬升面层，读起来像这张卡的一个图层而不是另一张卡），文字色走 `colorCaptionText`/`colorTitleText`，量规主色取 `colorPrimary`。**没有新写自定义 View** —— 量规直接复用空气质量卡在用的 `ArcProgress`（270° 弧 + 中心文字 + 底部文字），布局把 `item_details.xml` 从"图标+标题+内容"的整行改成"左侧标签/数值 + 右侧量规"的小卡，`DetailsViewHolder` 由 `LinearLayoutManager` 换成 `GridLayoutManager(2)` 并复用 `GridMarginsDecoration` 做间距（该 decoration 会给 RecyclerView 自身加 padding，**重复 add 会让 padding 每次变大**，故 bind 时先清空既有 decoration）。**量规只给有天然量程的读数**：湿度/云量 0–100、紫外线 0–11、气压按 950 起 130 跨度（只有标准气压两侧那一段带信息）、风速到蒲福 12 级（118 km/h，模型的默认单位是 km/h）、能见度到 20 km；露点与云底高没有天然量程，保留原来的图标放在同一位置，网格不会缺一块。**三处显示上的取舍**：① 风向由提供方给的是 `E`/`NE` 这种罗盘缩写而非本地化词（彩云转换器按角度查表），所以标签用「风况」、缩写放进量规中心、数值是风速 —— 不为此新造 16 方向 × 多语种的翻译；② 气压取整（1005 而不是 1004.82）、能见度一位小数，单位移进量规底部；③ 紫外线**不重复同一个数字** —— 有 level 文字（如"中"）时数值显示 level、量规中心显示指数，没有 level 时数值就是指数、量规中心留空。**信息损失（如实记）**：原卡片风那一行带「白天/夜间」两行风向风速，小卡放不下，现在只显示实时风；完整描述仍在无障碍朗读（contentDescription）里。**未加**「体感」小卡 —— 参考图里有，但原详情卡本来没有该项（主界面顶部已显示），属新增指标不在本次范围。**真机验证（MI 9，签名 release）**：六个小卡两列排布、单位文字不再压在弧线上、`logcat -b crash` 空；六变体 876 例 0 失败（本次无新增测试 —— 改动是布局与取值格式，逻辑判断都在 `ArcProgress` 与既有单位类里）。

- **详情小卡改成「左边文字分级 + 右边量规读数」，量规阴影改成扇形**。左侧大字不再是数字，而是**按数值分档的文字**，具体数值移进右侧量规中心：湿度 `干燥/舒适/潮湿/闷湿`（30/60/80）、气压 `偏低/正常/偏高`（1009/1022 —— 气压表刻度盘自古印的三段）、能见度 `很差/较差/一般/通透`（1/4/10 km）、云量 `晴/少云/多云/阴`（20/50/85%）、紫外线优先用提供方给的 level 文字、没有才用自己的 `弱/中等/强/很强/极强`（直接沿用 `UV.UV_INDEX_*` 的 2/5/7/10，正好是 WHO 分段）。**风况没造新词** —— 用 `Wind.getLevel()`（即 `CommonConverter.getWindLevel`，`wind_0..12` 在全部 26 个 locale 里早有译文；为空时自己再调一次兜底），风速进量规，风向缩写挂到标签上「风况 · ENE」（上一版缩写占着量规中心，数值搬进来后给它腾位置）。新增 20 条分级文案只写了 **values（英）+ zh-rCN/rHK/rTW**，其余 locale 按 Android 惯例回落英文。档位判定读**模型自己的单位**（%/毫巴/km），与用户选的显示单位无关；分档函数返回 `@StringRes int`（纯函数、不吃 Context），配 `DetailsLevelTest` 6 例钉住每条边界并验证各档文案互不相同。左侧文字加 `autoSizeTextType=uniform`（min 12sp）—— 中文两字宽松，英文 `Overcast`/`Very high` 会超宽，原 `ellipsize` 留着当最后兜底。**`ArcProgress` 阴影两处改动**：① 渐变由 `LinearGradient(0,top→0,bottom)` 换成 `RadialGradient`（圆心透明 → 弧线处着色，不压暗中心那个数字），并加 `mRectF.width()<=0` 早退（径向渐变半径必须为正，否则构造就抛）与 shader 为 null 时不画（否则 `mShadowPaint` 会用默认黑色糊一块）；② **阴影形状**由 `drawArc(useCenter=false)` 的**弦切片**换成 `useCenter=true` 的**扇形** —— 前者被一条垂直的弦切出硬直边（真机放大后量规中间那道竖线就是它，「阴影是垂直的」指的是这个，只换渐变方向治不了），后者从圆心按弧的角度扫出扇形、随读数长大；顺带删掉只为弦分支存在的 `progressEndAngle`/`deltaAngle` 和 `>=360`/`>180` 两个分支（净减代码）。**副作用（如实记）**：`ArcProgress` 与空气质量卡共用，那张卡的阴影一起变扇形；且低进度（原先约 <15%）以前**根本不画**阴影，现在会画一小片扇形（AQI 38/500、风速 2.1/118 都属此列）。**真机验证（MI 9，签名 release）**：白天主题下看六个小卡的分档文字（二级/闷湿/中等/偏低/通透/阴）与量规数值（2.1/99/3/1005/17.5/96）、夜间主题下放大看扇形阴影无硬直边（气压、紫外线两个 partial 量规）、`logcat -b crash` 空；六变体 912 例 0 失败。

- **详情小卡底色改成中性的抬升面层（原来偏粉/偏棕）**。原先小卡底色是 `getWidgetSurfaceColor(2dp, colorPrimary, colorSurface)` —— 拿 `colorPrimary` 当抬升叠色，真机实测出来是**暖色**：白天 `#F7EEF2` 压在中性卡片 `#FDFBFF` 上（R>B>G，明显发粉）、夜间 `#312222` 压在 `#1C1B1F` 上（R 49 / G 34 / B 34，明显发棕），与这张卡以及它周围所有卡片的色系不搭。**根因不是"叠色太重"而是"叠的是主色"**，且主界面卡片底色 `colorMainCardBackground` 是**固定的中性色**（白 `#FDFCFF` / 夜 `#1C1B1F`），而 `colorPrimary`/`colorSurface*` 在 API 31+ 会走动态取色（本机壁纸偏暖，所以设置页那些列表项本身就是暖棕的 —— 那里不冲突，主界面才冲突）。**改法**：仍用同一个 `getWidgetSurfaceColor` + `DEFAULT_CARD_LIST_ITEM_ELEVATION_DP`（App 自己的"卡上列表项"惯例，不新造方法），但叠色换 `colorOnSurface`、底色换成小卡真正坐着的那层 `colorMainCardBackground` —— 只抬亮度不弯色相，且随动态取色一起走。实测：白天 `#FDFCFF → #EEECEF`、夜间 `#1C1B1F → #2A272A`，均为中性灰。**中途试过 `colorSurfaceVariant` 直取，废弃** —— 夜间是 `#46464F`（动态取色下更亮更暖），一步跳太多，把 `colorCaptionText` 的标签字和量规底部单位字压成几乎看不见（真机截图确认），故退回轻量叠色。**真机验证（MI 9，签名 release）**：亮色/暗色两个主题各看一遍（临时把「设置 → 暗色模式」切到亮色主题验完**已恢复成"自动"**），取色确认中性、标签与单位字可读、`logcat -b crash` 空。

- **扇形阴影改成真正会"变深"的阴影（叠色由主色换黑色）**。`ArcProgress` 的阴影渐变一直是「圆心透明 → 弧线处 `colorPrimary` 10% 透明度」，方向本来就是由内向外，但**叠的是弧线自己的颜色**：亮色主题下主色深、压在浅底上确实越往外越深（实测 `#EEECEF` 底、`#E7E6E8` → `#D2D0D3`），**暗色主题下主色是浅粉**（弧线实测 `#FFB3AF`），压在深底上越往外越**亮** —— 出来的是一圈发光而不是阴影（实测 `#2A272A` 底、圆心 `#2A272A` → 弧线处 `#3C3335`）。改成 `Color.BLACK` 当叠色，两个主题都只做减法；顺带把两个 `SHADOW_ALPHA_FACTOR_*` 从都是 0.1 调成 **亮 0.14 / 暗 0.30**（深底上黑色叠色要更重才看得出）。实测改后：暗色圆心 `#282528` → 弧线处 `#1F1D1F`（底 `#2A272A`）、亮色 `#E7E6E8 → #D2D0D3`，两个主题都单调变深。**顺手删掉 `mShadowColor` 与 `getDarkerColor()`** —— 前者只被写从不被读（`onDraw` 只用 `mShaderColor`），后者只为喂它而存在，是上游带来的死代码。空气质量卡共用该 View，其阴影也从「绿色发光」变成「黑色阴影」，真机确认观感正常。**真机验证（MI 9，签名 release）**：暗色主题沿半径逐点取色确认单调变深、亮色主题同样确认（临时切换的暗色模式设置**已恢复为"自动"**）、空气质量卡单独看过、六变体 912 例 0 失败。

- **3.5.10**：**正式版**（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。汇总自 3.5.9 以来的三件事，全部围绕「详情数据」卡：① **改成两列小卡片网格并带量规**（复用空气质量卡的 `ArcProgress`，只给有天然量程的读数画弧）；② **左侧改文字分级、具体数值移进量规**（湿度/气压/能见度/云量/紫外线各自分档，风况复用既有的蒲福级译文；新增 20 条文案 4 个 locale），新增 `DetailsLevelTest` 钉住每条档位边界；③ **量规阴影从"垂直弦切片 + 主色叠色"改成"扇形 + 黑色叠色"** —— 前者在暗色主题下是发光而非阴影，后者由圆心向外单调变深，同时删掉 `mShadowColor`/`getDarkerColor()` 两处死代码。小卡底色由主色叠色改成中性叠色（原来白天发粉、夜间发棕，与主界面固定中性色的卡片不搭）。用例数 876 → **912**（六变体合计，152 例/变体）。**release 包真机验证**见上两条，0 崩溃。

- **英文 locale 实机验收，抓出两个缺陷；量规数字按显示单位定小数位**（3.5.10 发布后的收尾，未随任何版本发布）。**验证手段**：Android 13+ 的 per-app locale（`adb shell cmd locale set-app-locales <pkg> --locales en-US`），不动系统语言、不需 root，验完 `--locales ""` 清除覆盖 —— 比改系统语言安全，也是以后验多语言的标准做法。**① 「气压」标签在长译文下被截断**（英文显示「Atmospheri⋯」）：中文「气压」两字所以从未暴露，但 cs/es/fr/pl/ru/pt/it/el/ro/sl/hu **十一种译文都是"大气压"的长写法**，全部会截。两处一起改：英文基串 `Atmospheric pressure` → **`Pressure`**（全仓库只有详情卡引用该串，且与中文「气压」对齐），标签 `maxLines` 1 → **2** 让长译文换行（12sp 说明字再 autosize 缩就看不清，换行更可读；`ellipsize` 留作兜底）。**未改那十一种译文** —— 逐语言挑短写法属翻译工作，不靠猜。**② 风况档位被缓存冻结在取数时的语言**（切回中文后仍显示 `Light breeze`）：`Wind.getLevel()` 是**取数时按当时语言生成再存库**的文本，缓存活得比语言设置久；原写法"读存的、空了才现算"正好吃到这个坑。查过全部 7 个转换器，**每一个都是用 `CommonConverter.getWindLevel(speed)` 生成该字段**，没有提供方给自己的措辞 —— 存的字符串不含额外信息、只含一门语言，故改成**每次渲染按风速现算**，分支反而少一个。同类隐患仅此一处：湿度/气压/能见度/云量/紫外线的档位都是绑定时按数字算的。**③ 量规数字按显示单位定小数位**（原计划只修 atm，实测面更大）：原先气压 `Math.round` 让 0.99 atm → "1"、1.03 kgf/cm² → "1"；能见度固定一位小数在"米"下是 "10600.0"、英尺 "34776.9"。统一为 `gaugeNumber()`：**≥1000 取整 / ≥10 一位 / 其余两位**，用 `NumberFormat` 而非 `String.format("%.1f")` —— 前者跟随 locale 的小数分隔符**且**自动去掉空小数位（"10.6" 而不是 "10.60"），`setGroupingUsed(false)` 免得 72dp 圆里出现千分位。实测 1006 mb / 100.6 kPa / 754.4 mmHg / 29.7 inHg / 0.99 atm / 1.03 kgf / 10.6 km / 10600 m / 6.59 mi / 34777 ft。**测试自己抓出一处**：第一版规则是"≥100 取整"，kPa 徘徊在 100 上下会被抹掉约 1 mb，故改成 ≥10 就保留一位。`DetailsLevelTest` 6 → **10 例**（含德语 locale 小数点变逗号一例、字宽上限一例）。**变异检验**：小数位固定成 2 → 2 例 FAILED。六变体 912 → **936 例 0 失败**。**真机验证（MI 9，签名 release）**：英文下六张小卡文案完整不截（Light breeze / Humid / Moderate / **Pressure→Low** / Clear / Few）、数字 3.2 / 62 / 4 / 1003 / 14.2 / 39；清除 locale 覆盖后同一份英文时段缓存的风况显示回「二级」；`logcat -b crash` 三轮全空。

- **每日概览的「降水概率」把前七天的空缺从别的源嫁接过来**（多源聚合下）。现象：前 7 天没有降水概率标签、第 8 天起才有。**根因不在中国天气网缺数据这一点上，而在 `WeatherMerger.fillDaily` 拿的是 leader 的 `day()`/`night()` **整块**、从不填半日内部的字段** —— `ApihzResultConverter` 的每日半日写的是 `PrecipitationProbability(null,…)`（line 167），而 8..16 天是从 Open-Meteo **整条追加**的，自带概率，于是恰好在 APIHZ 的 7 天边界上断开。改法：`fillDaily` 对 day/night 各过一遍新的 `fillChanceOfRain()` —— leader 自己有概率就不动，没有才取同一天里第一个有概率的源。**只嫁接概率这一个字段**：温度、天气文案、降水量、风仍全部是 leader 的。**与既有设计规则的取舍（写进注释了）**：`WeatherMerger` 类注释明确写着「block-granular, never field-granular」，理由是"别渲染出『晴天』配另一个源的 5mm 降水"；概率是这条规则里最不冲突的字段（「多云，30% 概率」是正常预报，而「晴」配 5mm 自相矛盾），故按用户要求破例，且只破这一个字段。**同类空缺（已查明，未动）**：APIHZ 每日半日的 `Precipitation`（降水量）也是 null、`Wind` 速度写死 `0f`，所以前 7 天的「降水量」「风况」标签页同样缺数据 —— 嫁接这两项就是在动会与天气文案冲突的字段，性质与概率不同，留给用户定。测试 `WeatherMergerTest` 9 → **11 例**（一条验证概率被填入且温度/文案/降水量仍是 leader 的、一条验证 leader 自己有概率时不被覆盖，都用现成的 openmeteo/weatherapi 固件构造"无概率的 leader"）。**变异检验**：把嫁接改成直接 `return leader` → 第一条 FAILED。六变体 936 → **948 例 0 失败**。**真机验证（MI 9，签名 release）**：下拉刷新后横向滚完每日概览三段 —— 8-20~8-24 为 44/96/89/84/82%、7 天边界 8-25~8-29 为 58/48/56/49/52%、尾段 8-30~9-3 为 85/29/45/44/47%，全程连续无断点；`logcat -b crash` 空。

- **嫁接范围扩到「降水量」与「风」，`fillChanceOfRain` 改成 `fillHalfDay`**（按用户「嫁接」的指示，紧接上一条）。上一条只补概率，是因为概率不会与天气文案自相矛盾；用户要求把另两项也补上，**这是刻意把 block 规则掰弯的地方，如实记在注释里**：`Precipitation` 与 `Wind` 现在也按「leader 有就用 leader、没有才取同一天第一个有的源」填充，于是「中国天气网说晴、Open-Meteo 给 5mm」这种组合**变成可达的**了 —— 取舍是"大半张卡是空的"比"偶发矛盾"更糟；**温度与天气文案仍然只来自 leader**（这两个才是"预报本身"），类注释里那句"never field-granular"已相应改写成"block-granular by default"并指向 `fillHalfDay`。**每个子读数整块搬**：风是"方向与风速同时测得"的向量，所以搬整个 `Wind`（APIHZ 的每日风本来是"有方向字符串、degree 写死 0f、speed 写死 0f、level 由 0 反推成无风"，整块换掉严格更好），不做"这家的方向 + 那家的风速"的拼接。实现上直接复用既有的 `pick(candidates, valid)`（leader 排第一，故 leader 有效就赢），没新写取值逻辑。`cloudCover` 用 `firstNonNull` 一并填（仅入库、界面未显示）。测试 11 → **12 例**（新增一例用"APIHZ 形状"的 leader —— 概率/降水量都为 null、风速 0f —— 断言三项都被填且方向与风速来自同一个源，同时文案与温度仍是 leader 的；并给「leader 自己有的不被覆盖」那例补了风速断言）。**变异检验两轮**：分别把降水量、风改回不嫁接 → 新增那例各 FAILED 一次。

- **3.5.11**：**正式版**（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。汇总自 3.5.10 以来的两件事：① **英文 locale 实机验收修三项** —— 「气压」标签在十一种长译文下被截断（英文基串改 `Pressure`、标签允许两行）、风况档位被缓存冻结在取数时的语言（改为按风速现算）、量规数字按显示单位定小数位（`gaugeNumber()`，≥1000 取整 / ≥10 一位 / 其余两位，用 `NumberFormat` 跟随 locale 且去掉空小数位）；② **多源聚合下每日概览的降水概率/降水量/风嫁接进前七天** —— 根因是 `WeatherMerger.fillDaily` 从不填半日内部字段，而中国天气网这三项全空、第 8 天起的 Open-Meteo 整条又都有，恰好在 7 天边界断开。用例数 912 → **954**（六变体合计，159 例/变体）。**release 包真机验证**见上三条，0 崩溃。

- **修亮色主题下设置页返回箭头/关于按钮隐形、关于页头部被顶栏遮挡**（3.5.11 发布后用户报告，两处都是 Compose 设置界面）。**① 图标隐形**：`FitStatusBarTopAppBar` 的返回箭头与 `SettingsActivity` 的 ⓘ 都硬写了 `tint = MaterialTheme.colorScheme.onPrimaryContainer` —— 那是"画在 `primaryContainer` 上"的前景色，而顶栏容器是 `surface`；顶栏本身已把 `navigationIconContentColor`/`actionIconContentColor` 设成 `onSurface`（标题用的就是它，一直可读），硬写的 tint 把它盖掉了。**取色实测**：两个图标被画成 `#FFFFFF`、顶栏 `#FFF8F7`，对比度约 1.02:1。**只在动态取色的亮色主题下出现** —— `GeometricWeatherTheme` 在 API 31+ 走 `dynamicLightColorScheme(wallpaper)`，该机（MI 9/Android 14，暖色壁纸）的 `onPrimaryContainer` 落在近白；静态调色板里它是深藏青 `#001B3F`，所以**只读代码看不出来，必须真机 + 取色**。**改法是删掉这两个 tint**（而不是换一个颜色常量）：让图标继承顶栏自己的内容色，两种主题、动态与静态调色板下都不会再错。**② 关于页头部被遮挡**：`AboutActivity` 的 `Material3Scaffold(...) { }` 把回调参数 `innerPadding` 丢了，`LazyColumn` 只有 `fillMaxHeight()`，于是列表从 scaffold 顶端起排、整个头部（应用图标 + 名称 + 版本号）压在固定高度顶栏底下；补 `.padding(innerPadding)` 即可（`SettingsActivity` 本来就这么写，这里是漏的）。**真机验证（MI 9，签名 release，临时切到亮色主题复现、验完恢复"自动"）**：设置页 ← 与 ⓘ 为清晰深色、关于页完整显示图标与「几何天气 / 3.5.12_pub」、`logcat -b crash` 空。**未加测试** —— 两处都是 Compose 渲染层的颜色与 padding，本仓库没有 Compose UI 测试基建（Robolectric 只用于转换器/合并器），加一套 `createComposeRule` 只为断言 tint 不成比例；改动本身是"删掉一处覆盖 + 补一个既有参数"。

- **3.5.12**：**正式版**（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。只含上一条两个显示修复（亮色主题下设置页图标隐形、关于页头部被遮挡）。**3.5.11 线上那份带这两个缺陷**；另外 3.5.11 的 release 资产是 **CI 构建的那份**（本地验过的同名 APK 在修这两个 bug 时被覆盖，无法再上传同一二进制，如实记录）。3.5.12 的资产是本地构建并真机验证过的那份，sha256 与线上核对一致。用例数仍 **954**（六变体合计，159 例/变体；本次无新增测试，理由见上条）。

- **关于页加「检查更新」，GitHub 拆成两条（上游在上、本 fork 在下）**（3.5.12 之后，尚未随任何版本发布）。① 「概览」分组顺序改为 **检查更新 → GitHub（原作者）→ GitHub（本分支）→ E-mail**：原来只有一条 GitHub 且指向上游 `WangDaYeeeeee/GeometricWeather`，现在补一条本 fork `huaaaajjj/GeometricWeather`。② **检查更新**：新增 `settings/utils/UpdateHelper.kt`，调 GitHub `releases/latest` 取 `tag_name`/`html_url`，与 `BuildConfig.VERSION_NAME` 比对，用 `SnackbarHelper` 回报「已是最新版本」/「发现新版本 x.y.z」（后者带「去下载」动作跳 release 页）。**三处刻意的取舍**：**不写 Gson DTO 而用 `JSONObject`** —— DTO 放在 `weather.json.**` 之外会被 R8 剥掉字段名（现有 keep 规则只覆盖那个包），JSONObject 无反射、不必新增 proguard 规则，也不用碰 Retrofit/Hilt；**不做自动更新** —— APK 分发，下载安装留给用户；**版本比较必须按数字**逐段比 —— 本仓库正好会踩字符串序的坑（`3.5.9` vs `3.5.12` 反了），同时容忍 tag 的 `v` 前缀与 `VERSION_NAME` 的 `_pub`/`_gplay`/`_fdroid` 后缀。请求带 `User-Agent`（GitHub 对无 UA 请求返回 403），10s 超时，任何异常都退化成「检查失败」；`AsyncHelper.runOnIO` 拿到的 `Controller` 存字段，重复点击直接返回、`onDestroy` 取消。图标复用现成的 `ic_top`（向上对齐箭头），未新画 vector。新增 10 条文案（4 个 locale）。`UpdateHelperTest` **6 例**：补丁/次版本/主版本升级、相同版本（含三种 flavor 后缀）、缺省位补零（3.6 == 3.6.0）、垃圾输入（`nightly`/空串/`v`）不得报更新。**变异检验**：把 `isNewer` 改成字符串比较 → 3 例 FAILED。六变体 954 → **990 例 0 失败**。**真机端到端验证（MI 9，签名 release，R8 开）**：装 3.5.12 点检查更新 → 「已是最新版本」；临时把 `versionName` 降成 3.5.0 重新构建安装（`adb install -r -d`）→ 「发现新版本 3.5.12」+「去下载」，点击跳浏览器打开 release 页；版本号**已改回 3.5.12** 并重装正常包；`logcat -b crash` 全程空。

- **3.5.13**：**正式版**（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。只含上一条：关于页新增「检查更新」、GitHub 拆成「原作者 / 本分支」两条（上游在上）。**本版起用户可在应用内自己发现新版**（此前只能手动去 GitHub 看）。用例数 954 → **990**（六变体合计，165 例/变体）。

- **README 重写 + 英文版 + 真机截图**（3.5.13 之后，纯文档，不动代码）。原 README 停在 3.4.0：自称「勉强能用的修改版本」、天气源表里 AccuWeather 还写着「✅ 正常」、多源聚合/中国天气网/中国气象局三个源根本没提。重写为：截图 → 下载 → 天气源对照表（9 个源的 Key / 覆盖范围 / 数据量 / 状态）→ 多源聚合按块取数的解释 → 与上游的区别 → 构建与 flavor → 已知限制 → 许可证与致谢。新增 `README_en.md`（同结构，两文顶部互链）与 `docs/screenshots/` 五张截图（3.5.13 签名 release、MI 9：主界面、每日/小时概览、空气质量、详情数据、天气源弹窗）—— 截图里正好带着每块的来源标注，比用文字解释多源聚合省事。**逐条对着代码核过，改掉三处写错的**：① 中国气象局的数据量原写「7 天 · 56 小时」（从中国天气网那行抄串了），它是网页抓取的逐时表、条数随页面而定，按 AI_CONTEXT 的实测值改成「7 天 · 逐时（网页抓取）」；② flavor 表的定位一列原写 pub=高德 / gplay=Play Services / fdroid=系统定位 —— 实际默认项是 `native`（`SettingsManager.kt:120` 默认 `"native"`），pub/gplay 用 `src/proprietary` 那份 `AndroidLocationService`（有 Play Services 走 fused、否则退 `LocationManager`），高德/百度只是设置里可选的项；gplay 的 `AMapLocationService` 是空壳（`src/noamap`，选了会退回系统定位），fdroid 的选项列表被 `ServiceProviderSettingsScreen.kt:108` 裁到只剩「百度 IP + 系统」；③ 多源聚合的「每日」那条补上「国内源不给的降水概率/降水量/风从别的源嫁接进前 7 天」（3.5.11 做的事，原文漏写）。其余说法均已验证：QWeather/Visual Crossing 在源码里确已删净、`WeatherMerger.mergeAlerts` 确是所有源的并集、`mergeDaily` 确会把领导源 7 天之外的日子整条接上、五张截图的标题与正文说明一致。**未做**：README 不列版本号清单（每次发版就过期，指到 Releases 即可）；英文化只覆盖 README，`AI_CONTEXT.md`/`AGENTS.md`/`CLAUDE.md` 仍是中文。

- **删 Accu 的死重载与死接口方法 + `.gitignore` 收掉临时 worktree**（README 之后，纯清理，不改行为）。① `AccuWeatherService.requestLocation(Context, String, RequestLocationCallback)` 三参重载删除（27 行）—— 基类 `WeatherService` 只声明 `(Context, String)` 与 `(Context, Location, callback)` 两个（`WeatherService.java:50,52`），这个重载既非 override 也无调用方（按名搜索全仓库：`WeatherHelper.java:107` 走两参版、`LocationHelper.java:181` 与 `WeatherSourceStatusScreen.kt:265` 走 `(Context, Location, callback)`，测试里也只实现基类那两个），随它一起消失的是两个从未暴露的缺陷（`locationList.add(convert(...))` 缺 null 检查、`query.matches("[a-zA-Z0-9]")` 漏 `*` 只匹配单字符）—— **删掉比修好便宜**，这条路径本来就没人走。② `AccuWeatherApi.getWeatherLocation` 一并删除：它是 `callWeatherLocation` 的逐字复制（同 `@GET locations/v1/cities/translate.json`、同四个 `@Query`），刚删掉的那个重载是它唯一的调用方。③ `.gitignore` 加 `/.claude/worktrees/`（只挡临时 worktree，`.claude/skills/` 四个 SKILL.md 继续跟踪），并把已合并的 worktree `gauge-number-and-locale` 与同名分支删除 —— 该 worktree 被上个已退出的会话（pid 27484）锁着、`git worktree remove` 又撞上 Windows 长路径（`Filename too long`），故 unlock 后用 `\\?\` 前缀删目录再 `git worktree prune`。**验证**：`:app:testPubDebugUnitTest` 通过（165 例 0 失败 0 错误，28 个套件），main 源码编译无警告新增；未跑六变体 —— 删的是死代码，六份编译的是同一批 main 源码。**Accu 的 `cancel()` 仍是空操作**（等 Key，见「已知问题」），本次没动。

- **新增 MET Norway（挪威气象局）天气源**（Accu 清理之后，`docs/PORT_PLAN_breezy.md` 阶段 1）。动机：原先**全球覆盖且免 key 的只有 Open-Meteo 一个**，它一限流，非中国地区就只剩内置 Key 会被限流/已过期的 Accu、OWM 顶着。api.met.no 免 key 免注册，是第二条这样的路。**四层流水线**：`weather/json/metno/`（3 个 DTO，nowcast 与 locationforecast 共用 `MetNoForecastResult`，两者只差 `meta.radar_coverage` 与 `instant.details.precipitation_rate`）、`apis/MetNoApi.java`（4 端点）、`converters/MetNoResultConverter.kt`、`services/MetNoWeatherService.kt`（`RequestScope` + `async` 四路并行，只有 locationforecast 是必需的）。**接口怪癖，逐条都是坑**：① **UA 强制** —— api.met.no 的 ToS 要求每个请求带能识别应用与联系方式的 User-Agent，不带会被封，故在 `ApiModule.provideMetNoApi` 用 `client.newBuilder()` 加拦截器给它**单独一个 OkHttp 实例**（沿用共享超时/连接池），没动共享 client（CMA 绕 WAF 的先例）；② **没有日出日落** —— 整个 API 零 astro 字段，按方案 A 让 `Daily.sun()` 留空，`Weather.isDaylight()` 已自动退到 `DisplayUtils.isDaylight(timeZone)`（`Weather.java:146`），不崩，代价是自检页「日出」列为空 + 动态背景昼夜判断变粗；**逐小时的昼夜改读 `symbol_code` 的 `_day`/`_night` 后缀**（接口自己的答案，比时钟准 —— 奥斯陆 8 月 18:00 仍是白天，时钟法会判错），无后缀的基名（cloudy/fog/rain）才退回时钟；③ **没有 daily 块** —— locationforecast 只给一条扁平 timeseries（前 ~2.5 天逐时、之后逐 6 小时、最后一条**连预报块都没有**），日预报靠按**本地日历日**折叠：06:00–18:00 为昼、其余为夜。**故意不用 yr.no 自己的「18:00→次日 06:00」夜段**：那样在 00:00–06:00 刷新会把当天归到前一天，导致 `dailyForecast[0]` 是昨天，而全仓库 76 处把下标 0 当今天（与 `past_days=1` 同一类坑）。半天为空时（18:00 后刷新的今天白天、预报末尾的只有白天）用另一半兜底，保证 `day()`/`night()` 都非 null；半天的天气取**该时段最差的**（按 THUNDERSTORM>…>CLEAR 排序），不取正午那条，否则下午下雨会显示晴；降水量只累加与该点步长匹配的块（有 `next_1_hours` 用它、否则 `next_6_hours`），保证各点周期不重叠；④ **风速单位是 m/s**，本项目存 km/h，×3.6；⑤ **AQI 只有挪威境内**（巴黎都是 HTTP 400），且它的 `AQI` 字段是挪威自己的 1..4+ 档位号，**按「已知问题」的规矩弃用**，只取 µg/m³ 浓度走 `CommonConverter.getAqiIndexFromConcentration`（实测 pm25 3.11/pm10 5.60 → 中国 AQI 6；若原样写它的 1.75 会变成 2，空气再脏也恒绿）；无 SO2/CO；⑥ **nowcast 仅北欧**（北京 HTTP 422），给 5 分钟步长的分钟级降水，`RequestScope.execute` 已把失败降级为 null，拖不垮整次刷新；`dbz` 传 null —— 全仓库没有任何地方读它，换算 mm/h→反射率是死算术；⑦ **预警仅挪威**（境外 200 + 空 features），`awareness_level` 形如 `"2; yellow; Moderate"`，等级与颜色都在这一个串里；⑧ 时间戳有 `...Z`（预报/AQI）和 `...+00:00`（预警）两种，minSdk 21 没有 `X` 模式，统一归一化成 `Z` 能解析的 `+0000`（同 `provideMfWeatherApi` 处理 MF 的思路）。**注册点从 6 处变 7 处**：原 skill 漏了 `settings/compose/WeatherSourceStatusScreen.kt` 的 `SOURCES`，不加新源在「天气源可用性」自检页里是隐形的，而那正是验收要用的页面 —— 已把 skill 补成 7 处；同时给 `Src` 加 `nordic` 标志（测试坐标 → 奥斯陆 59.9139/10.7522），否则拿北京去探 MET Norway，AQI/分钟级/预警三列必空，分不清「超出覆盖范围」和「坏了」。**顺手删掉死配置 `CN_WEATHER_BASE_URL`**（`app/build.gradle` + `gradle.properties`，全项目除生成的 BuildConfig 外零引用）。**验证**：`weather/converters/MetNoResultConverterTest.kt` 新增 12 例（真实奥斯陆响应固件，2026-08-23T16:00Z 抓取 = 当地 18:00，正好落在「白天已过」这个兜底分支上；JVM 默认时区在测试里被钉到 `America/New_York`，折叠若退回默认时区会立刻失败），`:app:testPubDebugUnitTest` 全绿（177 例 0 失败 0 错误，原 165 例 + 12），`assemblePubDebug` 通过、Hilt 已生成 `ApiModule_ProvideMetNoApiFactory`/`MetNoWeatherService_Factory`。**release 包真机验证（MI 9/Android 14，由 3.5.13 签名包原地升级，保留原数据）**：`logcat -b crash` 全程空；「天气源可用性」实测（00:35，挪威气象局走奥斯陆坐标）该行 = **可用 / 11 天 / 87 点 / 体感 ✗ / UV ✓ / 气压 ✓ / 湿度 ✓ / 降水 ✓ / AQI ✓ / 预警 1 / 日出 ✗** —— 体感与日出为设计上的已知降级，AQI ✓ 与预警 1 证明挪威境内那两个端点端到端通了（当时确有一条黄色森林火险预警），11 天/87 点也和原始响应对得上；随后把当前位置（舒城县）切到该源，主界面正常渲染：27°「Fair」、风况 E 三级 3.7 米/秒、湿度 87%、UV 0 弱、气压 1004、露点 25℃、云量 17%、页脚「Powered by api.met.no」，**每日概览首列是「今日 8-24」** —— 这次实测恰好在当地 00:44 做的，正是 yr.no 夜段规则会把昨天顶到 index 0 的时间窗，实测没有发生；风速单位交叉核对：接口 3.8 m/s ×3.6 = 13.7 km/h 落在三级，界面按用户单位显示 3.7 米/秒，与「内部存 km/h」一致（若漏了 ×3.6 会显示一级）。验证后已把天气源改回「多源聚合」并确认恢复正常。**未做**：方案 B（给 `CommonConverter` 加 NOAA 日出日落算法，收益归所有源）留作后续；`docs/PORT_PLAN_breezy.md` 的阶段 2（小米天气）、阶段 3（FPAS 挂 COMPOSITE）未动；**天气文案是英文**（`weatherTextOf` 拼英文串，如「Fair」）—— 与 OPEN_METEO 一致的既有行为，未做本地化。

- **新增小米天气（XIAOMI）天气源**（`docs/PORT_PLAN_breezy.md` 阶段 2）。MIUI 天气 App 自己用的那套 `weatherapi.market.xiaomi.com/wtr-v3/` 端点，**免 key 免注册**（`appKey`/`sign` 是小米自己固定的一对，随 query 走，非按用户下发），中国境内数据来自彩云天气 + 中国环境监测总站、境外落到 AccuWeather 后端。收益：中国区它是**一次请求里最全的一个** —— current + 15 天 + 23 逐时 + 真实浓度 AQI + 预警，另有独立端点给 2 小时逐分钟降水（本项目此前只有 MF 与 METNO 有分钟级，且都不覆盖中国）；境外 5 天无 AQI，可当全球兜底。**四层流水线**：`weather/json/xiaomi/`（3 个 DTO）、`apis/XiaomiApi.java`（3 端点）、`converters/XiaomiResultConverter.kt`、`services/XiaomiWeatherService.kt`（`RequestScope`，先解析后并行取数）。
    **接口怪癖，逐条都踩过**：① **一次刷新是两步** —— `weather/all` 与逐分钟端点都要 `locationKey`（形如 `weathercn:101011600`），而只有 `location/city/geo` 能产出它；**本项目无处缓存**（Room schema 锁在 v63、`Location` 没有 per-source 参数表），塞进 `cityId` 会重演 3.4.13/3.4.14 的缓存键漂移，故按方案 A **每次刷新老实解析一遍**（一个小请求，换来无状态）。② **`isGlobal` 必须与 key 前缀一致** —— `weathercn:` 配 `isGlobal=false`、`accu:` 配 `true`，**传错不报错，返回 200 + 空预报**（与 CMA 的 `data:""` 同一类静默失败）；服务由前缀推导该标志。③ **几乎所有数字都是字符串**，缺值是 `""` 而不是字段缺失（`current.visibility` 常态如此），故一律 `takeIf { isNotEmpty() }.toFloatOrNull()`——直接 `toFloat` 会把缺值变成 0。④ **预报数组没有逐条时间戳**：日预报的日期取 `sunRiseSet[i].from` 的**日期**而非 `pubTime + i 天` —— 境外该数组**从昨天开始**（巴黎当地 02:31 抓取，`sunRiseSet[0]` 是前一天），按 pubTime 计数会整体错位，且已过去的日子必须**丢掉**（全仓库 76 处把 `dailyForecast[0]` 当今天）；逐时锚在 `forecastHourly.temperature.pubTime` + i 小时（由 `wind[i].datetime` 反向印证）。⑤ **`precipitationProbability` 比其余数组短**（15 天里只给 5 天），平行下标读取必须容忍越界。⑥ **AQI 是真的 0–500 中国指数**（不是档位号），可直接用，浓度公式只作兜底；`co` 是 mg/m³ 与 `AirQuality.getCOColor` 的阈值同量纲，不缩放。⑦ **风速本来就是 km/h**，是本项目里少数不用 ×3.6 的源。⑧ 预警的 `level` 是「蓝色/黄色/橙色/红色」这一个中文串，等级与颜色都从它推；`alertId` 是字符串而模型要 long，取 `hashCode()`。⑨ 时间戳带显式偏移（`+08:00`），一个后端还多给毫秒，minSdk 21 无 `X` 模式 → 去掉偏移里的冒号归一成 `Z` 能解析的 `+0800`（同 MF/METNO 的做法）。⑩ **locale 固定 `zh_cn`** —— market（中国）主机的英文文案是错的，小米自己的客户端也只在中文 locale 下用它；与 CMA/APIHZ 一致（那两个也只有中文）。
    **7 处注册点全过**（枚举追加在 METNO 之后、`WeatherServiceSet` 下标 10、`ApiModule` 用共享 OkHttp（无特殊 UA 要求）、`build.gradle` 三个 `buildConfigField`、`values/` 三数组 + `values-zh-rCN/` 两数组、自检页 `SOURCES`）。**验证**：`XiaomiResultConverterTest.kt` 13 例（真实抓取固件：北京 08:57 一份、带 4 条真实预警的 23:02 一份、巴黎 02:31 一份走 `accu:` 全球路径；JVM 默认时区钉在 `America/New_York`，折叠若退回默认时区立刻挂）+ `XiaomiWeatherServiceTest.java` 9 例（`ProviderServer` 跑真实 Retrofit/OkHttp 栈，锁**编排**：两步顺序、两个数据调用都带解析出的 key、`isGlobal` 与前缀一致、解析失败必须**停在第一步**不发数据请求、逐分钟端点挂掉不拖垮整次刷新、畸形载荷降级为 failed、cancel 后不得有迟到回调）。**变异检验**：把 `isGlobal` 写死 false → `anAccuKeyFlipsIsGlobalToTrue` FAILED，守卫是实的。`./gradlew test assemblePubDebug` 六变体全绿、**199 例 0 失败**（原 190，+9）。**未做**：**真机验收**（自检页那行 + 切到该源看主界面），下次真机冒烟时补 —— 端点已用真实请求逐个核过（两步流程与 `isGlobal` 组合都在 2026-08-24 实测），但 App 内端到端与 R8 下的表现**已于下面第二条补验**（2026-08-24 13:48/13:57，MI 9）；`docs/PORT_PLAN_breezy.md` 阶段 3（FPAS 挂 COMPOSITE）、阶段 4（`CommonConverter` 加太阳计算）未动；小米当次**没有挂进 COMPOSITE** —— 紧接着单独做了，见下条。

- **多源聚合的逐时块改由小米天气领衔**（紧接小米接入）。原领衔是 Open-Meteo，改动本体只有一处指派：`CompositeBlock.HOURLY` 由 `OPEN_METEO` 改成 `XIAOMI`，于是「每小时概览」卡的**温度、天气文案、风况**三样一起来自小米（这三样必须同源，见 `WeatherMerger` 类注释；小米的逐时是真有风向+风速的一对，Open-Meteo 也有，但一次刷新里国内的温度曲线以小米为准更贴近本地）。小米只覆盖约 23 小时，**之后的逐时仍由 Open-Meteo 整条追加**，序列长度不变（384 点），卡片标题的署名跟着 `CompositeBlock` 自动变成「小米天气」。**降水概率与降水量按老规矩补齐**：小米的逐时块这两项是空的（`PrecipitationProbability(null, …)` / `Precipitation(null, …)`），不补的话温度页的概率柱和降水页在前 23 小时会整段空掉，而追加段反而画得出来 —— 故 `mergeHourly` 照 `fillHalfDay` 的做法，**只在领衔者为空处**从第一个有值的成员移植（UV 本来就是这么补的）。这是**故意再弯一次分块规则**，代价与 `fillHalfDay` 相同：小米可能说「晴」而另一家给这小时 2 mm；判断仍是「空卡比不一致更糟」，温度与文案永远只来自领衔者。**小米在 `members` 里排最后**：它靠指派领衔逐时，若同时排到前面，未指派内容的兜底顺序会跟着变 —— 中国天气网第 7 天之后追加的日子会来自小米的 15 天而不是 Open-Meteo 的 16 天，那不是本次要改的。**顺带多出分钟级降水**：`merge` 取第一个带 minutely 的成员，国内即小米（其余四家都没有，彩云要正式 Token）。成员由 4 家变 5 家，多一次网络往返（小米自身是两步），仍在 30 s 单源预算内；小米失败/超时则整块退回 Open-Meteo，刷新不受影响。**验证**：`WeatherMergerTest` 新增 2 例（钉住「概率与雨量被移植」和「领衔者自己有值时不许被顶掉」，并反向断言温度/文案/风速风向没动），`CompositeBlockTest` 改断言 XIAOMI；`:app:testPubDebugUnitTest` **201 例 0 失败 0 错误**（原 199，+2）、`assemblePubDebug` 通过（Hilt 图接受新增的构造参数）。**未做**：真机验收（切「多源聚合」看逐时卡的温度/风况与署名），与小米源自身待补的真机验收一起做 —— **已于下一条补齐**。

- **小米源 + 多源聚合逐时块的真机验收**（MI 9 / Android 14，`assemblePubRelease` 签名包原地升级已装的 3.5.13，保留原数据，**R8 开启**）。补掉前两条各自欠的真机项。**主界面**（舒城县，13:48 下拉刷新）：「小时概览」卡署名为**小米天气**，页脚 `Powered by xiaomi.com + apihz.cn + caiyunapp.com + open-meteo.com`。**数值交叉核对**（同坐标同时段直连两家接口比对，这是唯一能证明「画的是谁的数」的办法）：界面 14/15/16/17 时温度 33/33/33/33°、风速 3.5/3.3/3.2/2.6 米/秒 ↔ 小米 `forecastHourly` 的 33/33/33/33 与 12.61/11.76/11.42/9.47 km/h（÷3.6 = 3.50/3.27/3.17/2.63，四对全中，**顺带证明没有二次 ×3.6**）；同时段 Open-Meteo 是 31.8/31.8/31.6/31.2° 与 21.3/21.6/19.4/17.5 km/h（=5.9/6.0/5.4/4.9 米/秒），与界面明显不符 —— 温度与风况确实取自小米，不是追加段。**降水概率**界面 13–17 时 31/41/38/28/18% ↔ Open-Meteo 同时段 31/41/38/28/18%，逐个相同，移植生效；降水量页 0/0/0/0.1/0.5 毫米也非空（小米这两项皆 null）。同时印证那条「故意弯规则」的代价真会出现：17 时图标是小米的晴，柱子却是别家的 0.5 毫米。**自检页**（13:57，北京坐标）：**综合** = 可用/16天/384时/体感✓/UV✓/气压✓/湿度✓/降水✓/AQI✓/预警0/日出✓（384 点说明 Open-Meteo 追加段没丢），**小米天气** = 可用/15天/23时/体感✓/UV✓/气压✓/湿度✓/**降水✗**/AQI✓/预警0/日出✓ —— 那一列读的是 `current.precipitation.total`（`WeatherSourceStatusScreen.kt:308`），小米当前时刻没有该字段，与「逐时缺降水」是两回事，均属已知。OpenWeather/AccuWeather 两行仍「不可用」（TLS 不稳 / Key 过期，见「已知问题」）。全程 `adb logcat -b crash` 为空。**仍未验**：小米的分钟级降水 —— 验收时无雨，分钟级卡片不出现，需等一次真降雨。

- **3.6.0**：**Prerelease**（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。**次版本号跳到 3.6 是因为这版加了两个天气源**（此前 3.5.x 都是单点修复）。汇总自 3.5.13 以来的五件事：① **新增挪威气象局（MET Norway）源** —— 免 key 全球，第二条不依赖 Key 的全球路；② **新增小米天气源** —— 免 key，中国境内一次刷新最全的一家（15 天 / 23 时 / 真实浓度 AQI / 预警 / 2 小时逐分钟降水），境外走 accu: 后端 5 天；③ **多源聚合的逐时块改由小米领衔**（温度 / 天气文案 / 风况取自小米，降水概率与雨量仍从其他成员移植），并因此让国内的分钟级降水可用；④ 删 Accu 的死重载与死接口方法、删死配置 `CN_WEATHER_BASE_URL`；⑤ README 重写 + 英文版 + 真机截图。**天气源从 8 个变 10 个**（加 COMPOSITE 共 11 项可选）。以上每条的坑与验证细节见前面各条。**真机验收**见上一条（MI 9 / Android 14，R8 开启，逐时数据与小米接口逐个对过、`logcat -b crash` 为空）。

- **调研并记录 breezy-weather 的非数据源借鉴项**（纯文档，未动代码，**记录待定不排期**）。前面 `docs/PORT_PLAN_breezy.md` 的四个阶段只看「多接哪个源」；这次读 breezy 的 README 与 `docs/SOURCES.md`，挑的是**架构与功能方向**，10 项按收益÷成本排进该文档新增的「阶段 5+」一节。**最值得先做的两件**：① **Open-Meteo 的空气质量端点**（`air-quality-api.open-meteo.com/v1/air-quality`，免 key，2026-08-24 拿巴黎实测过 —— 全球给 pm2.5/pm10/co/no2/so2/o3 浓度、欧洲另给 6 种花粉 grains/m³）。收益有两层：**仓库里那套 7 个文件的花粉 UI 当前对所有用户都是死的**（唯一供数方是 Accu，而它的 key 已过期），接上后 tree/grass/ragweed 三个槽能填、零新 UI 零 schema；同时 **Open-Meteo 从此有 AQI**（它现在完全没有，正是 COMPOSITE 必须拉彩云/WeatherAPI 的原因），且该端点给的是浓度、直接喂 `getAqiIndexFromConcentration`，符合「档位号不得原样写入」那条约束（它的 `european_aqi`/`us_aqi` 恰是不能用的那类，弃用）。② **把 `CompositeBlock` 的分块指派从硬编码改成用户可配** —— breezy 的核心架构就是分要素选源，而本仓库 3.5.9 的 `CompositeBlock` 已经是同一张表，只差读 `SharedPreferences` + 设置页几个下拉，**不碰 schema**。另记了「源能力/覆盖范围静态声明表」（可让 COMPOSITE 不再发注定失败的调用：METNO airquality 境外 400、nowcast 境外 422、小米境外 `status: -2`）、「每个地区各自选源」（`LocationEntity.weatherSource` 本就是强类型列，3.4.4 为修 bug 放弃的能力）、Gadgetbridge 广播、per-source 参数缓存走 prefs（小米 `locationKey` 现在每次刷新重解析）等。**明确不做**并记了理由：M3 Expressive 全量重绘（撞「保持原 UI 风格」）、多模块拆分 + kotlinx-serialization 换 Gson（撞现有 Gson DTO 与 R8 keep 规则）、Normals（要新表，schema 锁 v63）、雷达（breezy 自己还在可行性研究）、移除崩溃上报。

- **「检查更新」加预发布通道**（3.6.0 之后，尚未随任何版本发布）。**起因是 3.5.13 那个检查更新其实看不见本分支的日常版本**：它调的 `releases/latest` 按 GitHub 的设计**跳过 prerelease**，而本仓库的每日构建恰恰是以 Prerelease 发的（发布流程见「发布」一节）—— 2026-08-24 实测两端点即为此事的证据：`releases/latest` 给 `v3.5.13`（8-20，正式版），`releases?per_page=1` 给 `v3.6.0`（今天，`prerelease: true`）。**做法**：`UpdateHelper` 加第二个端点 `releases?per_page=1`（列表接口按时间倒序，`per_page=1` 只取头一条），`fetchLatest(prerelease: Boolean)` 按开关选端点；取 tag 的逻辑抽成 `parse(body)` 并**按首字符嗅探形状**（`[` 开头当数组，否则当对象），不额外传标志位 —— 这样「问了列表却当对象读」在结构上不可能发生。UI 上「检查更新」从 `aboutAppLinks` 数组里拿出来单独成行（它是唯一带尾部控件的一条），行内是标题 + 当前通道摘要 + 右侧 `Checkbox`；点行仍是执行检查，勾选只改通道并落 `SharedPreferences`（`SettingsManager.isUpdateCheckIncludingPrerelease`，**故意不发 `notifySettingsChanged`** —— 那个事件会让 MainActivity 重建重绘，选更新通道没有理由触发它）。Checkbox 无可见标签，用 `semantics { contentDescription }` 把「包含预发布版」给读屏。新增 3 条文案 × **4 个 locale**（values / zh-rCN / zh-rHK / zh-rTW，与 3.5.13 那批同范围）。**仍不做自动更新**，下载安装照旧留给用户。**测试**：`UpdateHelperTest` 6 → **12 例**，新增 6 例全打在 `parse` 上（对象形状、数组形状且 `prerelease: true` 不得被过滤掉、数组只取头条、前导空白不得让数组被认成对象、无 tag / 空数组 / `{}` 一律 null、缺 `html_url` 退到 `RELEASES_PAGE`）。**这些用例必须跑 Robolectric**：`org.json` 在 android.jar 里是桩，而本项目开了 `unitTests.returnDefaultValues`，桩会安静地返回 null 而不是抛 —— 不挂 runner 的话，一个空实现的 `parse` 也能「通过」。Robolectric 4.12.2 没有 SDK 35 沙箱，`@Config(sdk = [34])` 钉在 34。**变异检验两轮**：去掉 `trimStart()` → 前导空白那例 FAILED；把形状嗅探短路成恒假（只当对象读）→ 4 例 FAILED。六变体 `./gradlew test` **1242 例 0 失败**（207/变体）。**真机端到端已于 3.6.1 发版时补验**（临时构建 versionName=3.5.13 的同代码包：通道关 →「已是最新版本」、通道开 →「发现新版本 3.6.0」+「去下载」，见 3.6.1 条目）。**顺带记一个环境坑**：`compilePubReleaseKotlin` 报了一屏 `Unresolved reference: Material3CardListItem` 之类**全是假的** —— pubDebug 同源码编译通过，根因是 release 变体的 Kotlin 增量缓存脏了（`kaptGenerateStubsPubReleaseKotlin` 在源码已改的情况下仍 UP-TO-DATE；上一次 release 构建被中断过，还留了个 14:08 起的 Gradle Test Executor 僵尸进程占着 `R.jar` 让 `processPubDebugResources` 删不掉）。杀掉僵尸进程 + 删 `build/kotlin/compilePubReleaseKotlin`、`build/tmp/kotlin-classes/pubRelease`、`build/tmp/kapt3/stubs/pubRelease`、`build/generated/source/kapt/pubRelease` 后一次通过。**下次看到「整个文件所有项目内引用都解析不了」，先怀疑增量缓存，别改代码。**

- **3.6.1**：**正式版**（`assemblePubRelease` 已签名 + R8 minify/shrinkResources）。只含上一条「检查更新加预发布通道」与纯文档两件事（breezy-weather 借鉴项记录，未动代码）。用例数 1242（六变体合计，207/变体）。**真机验证（MI 9/Android 14，R8 开启）**：冷启动 0 崩溃、`logcat -b crash` 全程空；下拉刷新真实联网（更新时刻随刷新更新，R8 下 Gson DTO 完好）、定位显示舒城县；**检查更新双通道判别测试**（临时构建 versionName=3.5.13 的同代码包装载，`adb install -r -d`）——通道关（`releases/latest`）报「已是最新版本」（当时最新正式版 v3.5.13 == 已装），通道开（`releases?per_page=1`）报「发现新版本 3.6.0」带「去下载」动作按钮，正是上一条欠的那项端到端验收；通道摘要「仅正式版 ⇄ 包含预发布版（每日构建）」切换正确且跨重装持久化。验证后已恢复 versionName 并重装 3.6.1 正式包、设备动画设置复原。

- **接入 Open-Meteo 空气质量/花粉端点（`PORT_PLAN_breezy.md` 借鉴项 B 落地）**。`air-quality-api.open-meteo.com/v1/air-quality`，免 key，与预报是**两个主机**。一次买到两个收益：① **复活整套已存在但对所有用户都死着的花粉 UI**（7 个文件的唯一供数方是 Accu，其内置 Key 已过期）——欧洲区给 alder/birch/grass/olive/ragweed 五种花粉的逐时浓度（grains/m³），零新 UI、零 schema（`Daily.pollen` 槽本就存在）；② **Open-Meteo 从此有 AQI**（它此前完全没有，是多源聚合必须拉彩云/WeatherAPI 补空气的原因）——端点给的是浓度，走 `CommonConverter.getAqiIndexFromConcentration`（HJ 633-2012）换算中国 AQI，符合「档位号不得原样写入」约束，它自带的 `european_aqi`/`us_aqi` 恰是不能用的档位号，**未请求**。**实现**：新 DTO `OpenMeteoAirQualityResult` + 新 `OpenMeteoAirQualityApi`（独立 Retrofit，`OPEN_METEO_AIR_QUALITY_BASE_URL` 进 build.gradle）+ `ApiModule` 一个 provider；`OpenMeteoWeatherService` 改两路 `async` 并行，**空气质量是可选路**——503/超时由 `RequestScope.execute` 降级为 null，拖不垮整次刷新（与 OWM 的 air_pollution 同语义）。**接口怪癖（实测）**：① `forecast_days` 上限 **7**，传 8/16 直接报 `error:true` 而非截断，故单独常量 `AIR_QUALITY_FORECAST_DAYS = 7`；② 境外点请求照常 200、花粉列全 null（北京实测），折叠成无效卡即可；③ `current` 块的 `time` 形如 `2026-08-28T15:00` 带本地时区。**请求字段只声明消费的**：current 六项浓度（pm2_5/pm10/co/no2/so2/o3），hourly 只请求五种花粉（mugwort 模型无槽不请求，逐时 pm 列无人读不请求）。**换算两处**：CO 由 μg/m³ ÷1000 进模型（默认单位 mg/m³，`AirQuality.getCOColor` 阈值同量纲）；花粉 index 在边界 `roundToInt`（`Pollen` 模型的 index 槽是 Integer，Accu 供的浓度本就是整数）。**花粉折叠**：逐时浓度按时间戳的**日期前缀**归到每日最大值——预报与空气质量两端点请求的是同一个 timezone 参数，`daily.time` 与逐时前缀精确匹配，无需日期解析；`tree` 槽取三树种中**档位最高**者、其峰值浓度随行（单一树种的极值不被另两树种平均掉）；`mold` CAMS 无对应字段、恒空。档位阈值 **Atmo France**（breezy-weather 同表，同为 LGPL-3.0）：alder/birch 0/10/60/100/500、olive 0/20/100/200/500、grass/ragweed 0/3/30/50/250 → level 0..4，配新文案 `pollen_level_none/low/moderate/high/very_high`（values + 三个中文 locale）。**沿用既有语义**：index=0 且 level=0 的槽 `isValid()` 判 false（Accu 同款），整日无花粉时卡片不显示；description 与 aqiText 一样是**取数时按当时语言固化进缓存**的既有模式（3.5.11 修过的风况冻语是「有纯数值可现算」才改的，这里无可现算的数值档位）。**与多源聚合的衔接**：`WeatherMerger` 的 AIR_QUALITY 块搬的正是 `Current.airQuality`，把空气指派给 Open-Meteo 即可用（默认指派未改）；花粉由 `fillDaily` 的补空机制自动从 Open-Meteo 撒进中国天气网领导的 7 天。**验证**：固件巴黎 + 北京真实抓取（2026-08-28，forecast_days=3），**日期标签平移到与 forecast.json 的三天对齐**（形状/数值未动，测试注明）；转换器 +4 例（北京 pm2.5 120.1→AQI 157/aqi_4/CO 2.692 mg/m³、巴黎花粉逐日聚合 1.4/5.0/6.4 与档位文案、境外花粉无效卡、无空气质量时退化为原行为——顺带覆盖 3 参 `@JvmOverloads` 重载）、服务 +2 例（`forecast_days=7`、空气质量 503 不拖垮刷新），既有服务用例更新（两路并行 requestCount=2 + AQI 157 端到端）；**变异检验**：删掉 CO ÷1000 → `currentAirQualityIsDerivedFromConcentrations` FAILED，恢复后绿。`./gradlew test` 六变体全绿 **213 例/变体**（原 207，+6），`assemblePubDebug` 通过。**真机验证（MI 9/Android 14，签名 release 原地升级 R8 开启）**：冷启动 0 崩溃、`logcat -b crash` 全程空；切 OPEN_METEO 源（舒城县）下拉刷新成功，头部显示「空气质量 - 优」，**空气质量卡完整渲染 AQI 21 / 优**，分项 PM10 15.3 / SO₂ 5.7 / NO₂ 28.3 / O₃ 71 微克/米³、**CO 0.3 毫克/米³**（μg/m³ ÷1000 的换算在 R8 下生效），页脚 `Powered by open-meteo.com`；舒城无花粉卡（境外无效路径按预期工作，欧洲坐标下的花粉卡待真实欧洲坐标或来年花粉季验证）；**「天气源可用性」实测（22:32，北京坐标）：OPEN_METEO 行 AQI 列由恒 ✗ 变为 ✓**，16 天/384 时、体感/UV/气压/湿度/降水/日出全 ✓；验证后切回「多源聚合」确认恢复正常（彩云文案、中国天气网日、本地预警照常）。同轮 WeatherAPI 显示不可用（已知配额/网络抖动）、OpenWeather 那轮 AQI 列 ✗（该源 air_pollution 调用偶发超时，与本改动无关）。**遗留（未做）**：花粉卡需欧洲坐标端到端看一眼（当前设备在中国，只能验证到「境外正确不显示」）；多源聚合把 AIR_QUALITY 指派给 Open-Meteo 的效果未在真机试（指派仍是硬编码，属借鉴项 A）。

- **小时概览裁到三天**（按用户要求，3.6.1 之后）。多源聚合的小时序列长达 384 点（Open-Meteo 追加段），第四天往后纯属滚动长度而非信息。做法沿用 3.5.9「小时概览锁到当前时间」的同一入口：`Weather` 新增与 `withHoursFrom` 对称的 `withHoursUntil(time)` —— 丢弃 `time` 起点及之后的小时，**恰好落在 horizon 上的那一格也丢**，同样保留「无可丢/全可丢都原样返回」的双逃生门（全丢时宁可显示旧序列也不给空卡）；`HourlyViewHolder` 在卡片入口链式调用 `withHoursFrom(now).withHoursUntil(now + 72h)`，一张卡的裁剪连同趋势标签页一起生效（趋势适配器都从传入的 location 读数据）。**数据层不动**：库里仍存各源完整小时序列，自检页的「384 时」计数不受影响；widget/通知不经过此入口，也不受影响。**验证**：`WeatherTrimTest` 5 → 7 例（恰在 horizon 上的格必须丢、前一格必须留；序列完全在 horizon 内/外都原样返回）；**变异检验**：把 `withHoursUntil` 短路成空操作 → 边界用例 FAILED。`./gradlew test` 六变体全绿 **215 例/变体**（213 + 2）。**真机验证（MI 9，无压缩 release 验证包）**：小时卡从当前小时起、滑到 **9-1 0时** 即到底（当时 8-29 00:33，now+72h ≈ 9-1 00:33，边界精确），`logcat -b crash` 空。**环境坑（重申并扩充）**：这台开发机夜间空闲内存只剩 1.3~2.3G，`assemblePubRelease` 的 R8 阶段反复把 Gradle 进程顶死（连 hs_err 都不写、被系统直接终止），`--no-daemon --max-workers=1 -Xmx1536m` 也不保证成，先杀掉残留的 Kotlin/Gradle 守护进程再试能过；本次为出验证包**临时**关了 minify/shrinkResources（`git checkout -- app/build.gradle` 已还原、未入库）——本次改动是纯显示层裁剪、无反射/Gson 风险，非压缩包验证行为足够；**正式发版前必须在内存宽裕时重出 R8 包并冒烟**。

- **主页聚合来源核对（2026-08-29 00:42 刷新，舒城县，同坐标直连四家接口逐项比对）**。结论：**五个卡的来源标注全部正确，没有任何块被错标到实际失败的源**。① 每日概览 · 中国天气网 —— 今日晴 33/24 = apihz 嵌套 **day2**（2026/08/29 晴 33/24），8-30~9-2 = day3~6（大雨 28/21、大雨 26/21、小雨 25/20、阴 26/21）逐项吻合；接口根上平铺的 day1（weather1=小雨/33/**25**）实为**滞后的 8-28**，「day1 日期由 day2 推导 + withDaysFrom」恰好把它跳过 —— 3.5.6 的锚定策略在实战中工作正常；90/98/90/90/63% 的降水概率来自 Open-Meteo 嫁接（设计内）。② 小时概览 · 小米天气 —— 1 时起逐点与小米精确吻合（27/27/27/26）；**唯一例外：0 时一格（26°）整条来自 Open-Meteo 补位**（小米逐时锚 pubTime=下一个整点、列表从 1 时起，0 时桶由 mergeHourly 从首个有该桶的成员整条填补，温度图标文字随补位源；实测 OM 0 时 26.5°→界面 26°）。③ 空气质量 · 彩云天气 —— AQI 16 / PM2.5 4 / PM10 16 / SO₂ 8 / NO₂ 16 / O₃ 36 / CO 0.4 与彩云 realtime 逐项一致。④ 详情数据 · 彩云天气 —— 湿度 99 / 气压 1001 / 能见度 10 / 风向 N 为彩云值（风速 2.1 vs 直连 2.3 为 5 分钟观测漂移）；**云量 60% 实为 Open-Meteo 供给** —— 彩云转换器**从不映射 cloudrate**（结构性空缺，非当次失败），mergeCurrent 的 firstNonNull 落到 OM；紫外线 0 同理由 OM 补（该次彩云 life_index 为空，日间非空时仍会是彩云）。⑤ 日月升落 · 中国天气网 —— 05:46/18:40 = apihz suntimes[0]（05:46:08/18:40:33）；小米同值，无可区分。页脚 `Powered by xiaomi.com + apihz.cn + caiyunapp.com + open-meteo.com` ✓。头部晴 27°/体感 31° = 彩云 current（26.87→27、30.7→31）。**记录两个「标签内补位」的固有限制（均属 3.5.9/3.6.0 既定取舍，非 bug）**：小时卡首格、详情卡的云量（永久）与紫外线（彩云缺 life_index 时）来自其它成员而署名仍写指派源；另外彩云直连的坐标是**经度在前**（`/{lng},{lat}/`），纬度在前会报 `latitude out of bounds`。

## 已知问题 / 约束

- **三个源没有日出日落，退到写死的 06:00–18:00**（2026-08-24 查明，**已决定暂不修**）：`METNO`（整个 API 无 astro 字段）、`CMA`（DTO/转换器里 `sun` 零匹配；上游响应里有没有**未能核实** —— WAF 掐本机 TLS 握手）、`OWM`（**接口其实给了却没读** —— `OwmCurrentResult.SysBean.sunrise/sunset` 已在 DTO 里，转换器只读了 `sys.pod`；`/data/2.5/forecast` 的 `city.sunrise/sunset` 连 DTO 都没声明。属「抓了不用」，同 `air_pollution`/Open-Meteo `sunrise` 那一类）。后果不只是「日月升落」卡被 `MainAdapter.java:88` 整块跳过：`Weather.isDaylight()` 在 `sun()` 为 null 时退到 `DisplayUtils.isDaylight(timeZone)`，那是**写死的 06:00–18:00**（`DisplayUtils.java:210-217`），而它喂主界面配色、动态背景、全部 widget、通知、磁贴、快捷方式、AQI/城市卡明暗（`location.isDaylight` 十余处消费点）。**中低纬度看不出来**（北京/舒城误差半小时内），**高纬度每天错几小时** —— 奥斯陆 12 月 09:18–15:12、6 月 03:54–22:54，而 METNO 正是北欧源。要修的顺序：先读 OWM 那个现成字段（两行），再考虑给 `CommonConverter` 加 NOAA 太阳计算（收益归 CMA/METNO 与将来的新源，极昼极夜必须返回 null 而非硬凑）。COMPOSITE 不受影响 —— `WeatherMerger.fillDaily` 会从别的成员补一个有效 `Astro`。
- **FPAS（FOSS Public Alert Server）已决定不接**（2026-08-24）：理由与更省的替代路径记在 `docs/PORT_PLAN_breezy.md` 阶段 3/4 的裁决一节。
- **`AccuWeatherService` 的 `cancel()` 仍是空操作**：其余 7 个服务已迁 Kotlin 并通过 `RequestScope` 真正实现取消（取消在途 `Call` + 回调前查 `isActive`），只有 Accu 还是旧的 `AsyncHelper.Controller` 写法 —— `Job.cancel()` 打不断阻塞的 `execute()`，回调照常送达。因 Key 过期抓不到固件、无法写服务测试，按「无测试不迁」保留。它同时是 `weather/services/` 最后一个 Java 文件（`WeatherService` 基类除外）。（其 `requestLocation(Context, String, RequestLocationCallback)` 三参死重载已于 README 之后那次清理中删除，见变更日志末条。）

- **`app/src/proprietary/AndroidManifest.xml` 是死文件，从未进过任何发布包**。`app/build.gradle` 的 pub/gplay 两个 sourceSet 写的是 `manifest.srcFile "proprietary/AndroidManifest.xml"`，AGP 按 **project 目录**（`app/`）解析 → 指向不存在的 `app/proprietary/AndroidManifest.xml`，少了 `src/` 前缀；缺失的 flavor manifest 不报错，静默贡献为空。**是上游 `1cca480` 带进来的**，本仓库历史上一直如此。证据链：① `manifest_merge_blame_file/pubDebug/**` 的 blame 报告里应用侧来源只有 `app/src/main/AndroidManifest.xml`；② 合并产物中 `com.baidu.lbsapi.API_KEY`/`com.baidu.location.f`/`com.amap.api.v2.apikey`/`APSService`/`requestLegacyExternalStorage` 全部不存在；③ 真机 `dumpsys package` 看 **已装的 3.5.4 正式包**，requested permissions 里没有 `WRITE_EXTERNAL_STORAGE`/`READ_PHONE_STATE`/`MOUNT_UNMOUNT_FILESYSTEMS`/`READ_LOGS`/`INSTANT_APP_FOREGROUND_SERVICE`。**推论**：① 3.4.2「Baidu/AMap 服务补 `foregroundServiceType=location`」实际从未生效；② 百度/高德的 AK meta-data 不在包里，而百度定位真机实测一直正常（3.4.10 南开区、3.5.4 舒城县）——该 jar 版本不依赖 manifest AK，`com.baidu.location.f` 未声明时退化为进程内定位；③ **不要**想当然地"修好 srcFile 就完事" —— 一旦接上，会**新增** `READ_PHONE_STATE`（dangerous，商店权限列表会多出「电话」）、`MOUNT_UNMOUNT_FILESYSTEMS`、`READ_LOGS` 三项，与"少要权限"的方向相反，且百度会改走 `:remote` 进程服务，属行为变更。要接必须同时按需裁剪该文件内容并重新真机验证定位。
- **AQI 一律用中国标准（HJ 633-2012 / GB 3095-2012）**：`AirQuality.aqiIndex` 是 **0–500 的中国 AQI 数值**，不是档位号；`aqiText` 走 `CommonConverter.getAqiQuality()`。新增源若接口只给「档位号」（如 us-epa-index 1–6、OWM main.aqi 1–5），**不得原样写入** —— 有浓度就走 `CommonConverter.getAqiIndexFromConcentration(pm25, pm10)`，没有才映射到档位中值。原样写档位号的后果是六档全落在 ≤50 的第一档，空气再脏也显示绿色。
- **转换器是高发崩溃区**：`weather/converters/*ResultConverter.java` —— provider 字段常为 null，`Weather` 模型 `@NonNull` 断言会抛；新增/改源必须逐字段兜底。
- **数据库 schema 锁定 v63，禁止修改**（`db/GeometricWeatherDatabase` line 33）。
- **Room 禁止主线程访问**，一律 `AsyncHelper.runOnIO`（历史多次崩溃来源）。
- **CMA**：weather.cma.cn WAF 拦截默认 okhttp UA（已加浏览器 UA，仅限 CMA）；其无坐标→站点接口，靠全国站点图找最近站点。**开发机上抓不到固件** —— WAF 对非浏览器的 TLS 指纹直接掐握手（curl `schannel: failed to receive handshake` / python `SSL: UNEXPECTED_EOF`，HTTP 也是 empty reply），手机上正常，故 CMA 与 ACCU（Key 过期）目前没有转换器测试。
- **APIHZ（中国天气网）**：主走 `tqyb.php`（`sheng`+`place`）按城市取数，`tqybip.php` 仅作 IP 兜底。接口名怪癖：`place` 去尾「区」、直辖市 `sheng` 去尾「市」；区级覆盖不全，按 区→市→IP 依次兜底（见 3.4.9）。海外地名查不到 → 退 IP（国外 IP 接口默认返回北京）。
- **ACCU 的 Key 已过期**（2026-06-28 实测：geoposition/currentconditions 均返回 403 `"This API Key has expired"`）→ AccuWeather 源在**内置 Key 下**完全不可用，需在 `build.gradle:30-32` 换新 Key（三个：`EMBEDDED_ACCU_WEATHER_KEY`/`EMBEDDED_ACCU_CURRENT_KEY`/`EMBEDDED_ACCU_AQI_KEY`，base64 编码；也可在 `local.properties` 覆盖，优先级更高）。本是功能最全的源（15天/24时/UV/AQI/分钟级/预警）。**3.5.2 起默认源已改为 WEATHERAPI**，避免新装用户落到死源；**天气源选择页已标注「不可用 —— 内置密钥已失效」**（仅在 `customAccuWeatherKey` 为空时显示）。**「高级 → 设置服务商 API KEY」入口已于本次移除**（见变更记录），自填 Key 的路径只剩 `local.properties` / `build.gradle`；`SettingsManager.customAccu*Key` 等读取逻辑保留，老用户此前存过的 Key 仍然生效。
- **CAIYUN 为试用 Token**（`HYMo2dWkbB73N7rt`）：daily 上限仅 **3 天**、无 minutely 分钟级降水块；realtime/daily 字段齐全（体感/UV(life_index)/AQI/能见度）。如需 15 天+分钟级须换正式 Token。预警 DTO/转换器已接通，但真机实测该试用 Token 即使带 `?alert=true` 仍返回 `result.alert=null`；代码按空列表正常降级、0 崩溃，真实预警需正式 Token 才能端到端验证。
- **各源数据丰富度实测**（COMPOSITE/OPEN_METEO/WEATHERAPI 一行为 2026-08-18 舒城县/北京实测，其余为 2026-06-28，MF用巴黎，METNO 为 2026-08-23 奥斯陆抓取的原始响应实测）：COMPOSITE 16天·384时·含AQI+预警(当时的指派：Open-Meteo 骨架 + WeatherAPI 补 AQI/预警；现行指派见变更日志末条 —— 逐时小米、日 APIHZ、现况/AQI 彩云，逐时/日超出部分由 Open-Meteo 追加)；OPEN_METEO 16天·384时·全字段+AQI(浓度换算，2026-08-28 起)+花粉(仅欧洲；无预警)；WEATHERAPI **3天·72时**·含AQI+预警(免费档只给 3 天，2026-06-28 记的 14天·336时 已失效)；METNO 约11天·约90点(前 2.5 天逐时、之后逐 6 小时，最后一点无预报块)·含UV/露点/气压/湿度/降水概率·**无体感、无日出日落**；AQI 与预警仅挪威、分钟级(5 分钟步长)仅北欧；XIAOMI（2026-08-24 北京/巴黎抓取的原始响应实测）中国 **15天·23时**·含真实浓度AQI+预警+**2小时逐分钟降水**·有体感/UV/气压/湿度（能见度常为空）、境外 5天·23时·无AQI/无分钟级/无预警；CAIYUN 3天·48时·含AQI/UV；APIHZ 7天·56时(逐3h)·无UV/AQI/能见度；CMA 7天·逐时(网页抓取)·无UV/AQI；OWM 5天/40点(3h步长)·有AQI·2.5无UV；MF(法)15天·73时。

- **METNO（api.met.no）**：① **每个请求必须带能识别应用的 User-Agent**，这是 ToS 硬要求，不带会被封 —— 已在 `ApiModule.provideMetNoApi` 用拦截器给它单独一个 OkHttp 实例，**不要把它的调用挪回共享 client**；② 四个端点里**只有 locationforecast 是全球的**，airqualityforecast 仅挪威（境外 HTTP 400）、nowcast 仅北欧（境外 422）、metalerts 境外返回空列表 —— 三者失败都由 `RequestScope.execute` 降级为 null，属预期而非故障；③ **无日出日落**（整个 API 零 astro 字段），`Daily.sun()` 留空，自检页「日出」列为空是已知降级；逐小时昼夜读 `symbol_code` 的 `_day`/`_night` 后缀；④ **无 daily 块**，日预报由 `MetNoResultConverter` 按本地日历日折叠（昼 06–18），**不要改成 yr.no 的 18:00→次日 06:00 夜段**，那会让凌晨刷新时 `dailyForecast[0]` 变成昨天；⑤ 无体感温度、无能见度。
- **XIAOMI（weatherapi.market.xiaomi.com/wtr-v3/）**：① **一次刷新必须两步** —— `location/city/geo` 拿 `locationKey`，再 `weather/all` + 逐分钟；**不要试图缓存那个 key**（Room schema 锁 v63，塞进 `cityId` 会重演 3.4.13/3.4.14 的缓存键漂移）。② **`isGlobal` 必须与 key 前缀一致**（`weathercn:`→false、`accu:`→true），**传错返回 200 + 空预报而不是错误**，是静默失败路径，已有服务测试钉住。③ **数字全是字符串、缺值是 `""`**，解析必须先判空串（直接 `toFloat` 会把缺值变成 0）。④ **预报数组无逐条时间戳**，日期取 `sunRiseSet[i].from`；**境外该数组从昨天开始**，转换器会丢掉已过去的日子 —— 不要改成按 `pubTime + i 天` 计数。⑤ `precipitationProbability` 比其余日数组短（15 天只给 5 天），平行下标读取要容忍越界。⑥ 风速本来就是 km/h（**不要再 ×3.6**），AQI 是真的 0–500 中国指数、`co` 是 mg/m³，均直接采用。⑦ locale 固定 `zh_cn`（market 主机的英文文案是错的）。⑧ 境外无 AQI、无分钟级、无预警，`aqi` 块以 `{"status": -2}` 形式出现而非缺失。
- **OWM**：国内到 `api.openweathermap.org` 的 **TLS 握手不稳**（`connectTls`→`doHandshake` 偶发 30s 超时，三路并行冷启动随机某路中招），非代码缺陷。Key 有效。天气源可用性页的检测预算已由 25s 提到 35s，否则会把它误报成"不可用"。
- **空 daily 列表是全局隐含前提**：全仓库 **76 处** `getDailyForecast().get(0)` 不做保护（UI/widget/通知）。已在 `WeatherHelper.requestWeatherSuccess` 与 `DatabaseHelper.readWeather` 两个汇聚点拦截，新增消费点仍应假定「daily 至少一条」由这两处保证；**绕过这两处直接构造 Weather 的新代码必须自己判空**。
- minSdk 实际为 21（目标 24，暂不升级）。

## 待完成（TODO）

- [x] 升级 compileSdk 34 → 35
- [ ] 升级 minSdk 21 → 24（暂不升级，仅在关键库必须要求时升级）
- [x] GreenDAO → Room
- [x] 升级 Gradle 8.x
- [x] 升级 AGP 8.x
- [x] 升级 Kotlin 1.9+
- [x] RxJava → Coroutines
- [ ] Java → Kotlin 逐步迁移

## 重要文件引用

- `db/GeometricWeatherDatabase` line 33 —— `version = 63`（schema 锁定）
- `weather/WeatherServiceSet.java` —— `WeatherSource` → service 映射（11 项，含 COMPOSITE）。`get()` 是**手写下标**，新源只能追加到数组末尾
- `weather/apis/CmaApi.java` line 27 起 —— 三个接口的浏览器 `@Headers("User-Agent: …")`（绕 WAF 403）
- `weather/converters/*ResultConverter.java` —— null 防御集中地（崩溃高发区）
- `common/utils/helpers/AsyncHelper.kt` —— IO/UI 线程封装；Emitter 回调 post 回主线程
- `location/services/LocationService.kt` —— 定位服务（按 flavor 分实现），IP 兜底 `BaiduIPLocationService`
